import type { EntityType, CatalogEntityOperation, MandapamPipelineContext } from '../steps/context.types.js';import { entityCrudWithTranslations } from '../steps/entity-crud-with-translations.step.js';import { mandapamFlushCacheInvalidations } from '../steps/mandapam-flush-cache-invalidations.step.js';export async function catalogEntityPipeline(  entityType: EntityType,  operation: CatalogEntityOperation,  input: any,  id?: string,  cacheManager?: any,): Promise<Record<string, unknown>> {  let ctx: MandapamPipelineContext = {    input,    id,    performedBy: 'system',    entityType,    catalogOperation: operation,    cacheManager,    cacheEnabled: !!cacheManager,  };  const entityAction = operation === 'PUBLIC_LIST'    ? 'PUBLIC_LIST'    : operation === 'CREATE' ? 'CREATE'    : operation === 'UPDATE' ? 'UPDATE'    : operation === 'DELETE' ? 'DELETE'    : 'LIST';  ctx = await entityCrudWithTranslations(ctx, entityType, entityAction as any);  if (operation === 'CREATE' || operation === 'UPDATE' || operation === 'DELETE') {    ctx = await mandapamFlushCacheInvalidations(ctx);  }  return ctx.responseData as Record<string, unknown>;}