// ============================================// 📌 Clipboard Utility// Standardizes text copying across the application.// Used in Payment.tsx and potentially other data-rich components.// ============================================/** * Copies the provided text to the user's clipboard. * Includes basic error handling and success status return. *  * @param text - The string to copy * @param label - A human-readable label for the data (used for feedback) * @returns Promise<boolean> - True if successful, false otherwise */export const copyToClipboard = async (text: string, label: string): Promise<boolean> => {    try {        // Attempt to use the modern Clipboard API        await navigator.clipboard.writeText(text);        return true;    } catch (err) {        console.error(`Failed to copy ${label}:`, err);        return false;    }};