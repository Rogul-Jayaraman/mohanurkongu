import { useState, useEffect, RefObject } from 'react';/** * useIntersection hook for detecting when an element enters the viewport. * Used for lazy loading images and trigger animations. */export const useIntersection = (ref: RefObject<HTMLElement | null>, rootMargin: string = '0px'): boolean => {    const [isVisible, setIsVisible] = useState(false);    useEffect(() => {        if (!ref.current) return;        const observer = new IntersectionObserver(            ([entry]) => {                if (entry.isIntersecting) {                    setIsVisible(true);                    // Once visible, we can stop observing for lazy loading                    observer.unobserve(ref.current!);                }            },            { rootMargin }        );        observer.observe(ref.current);        return () => {            if (ref.current) {                observer.unobserve(ref.current);            }        };    }, [ref, rootMargin]);    return isVisible;};