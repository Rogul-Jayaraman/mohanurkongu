import { useState, useCallback, useEffect, useRef } from 'react';import { Profile } from '../types/profile';import { useInputFormatting, type InputFormattingMode } from './useInputFormatting';import { useIndexedDB } from './useIndexedDB';import { indexedDBStorage } from '../lib/indexeddb';import { formToDraft, draftToForm } from '../adapters/profile.adapter';import { getMinDobDate, getMaxDobDate, validateField, validateStepAtNav } from '../validation/profile-schema';import { translateError } from '../utils/translateError';export const DEFAULT_FORM_DATA = {    profileFor: 'MYSELF',    gender: 'MALE',    maritalStatus: 'NEVER_MARRIED',    diet: 'VEGETARIAN',    caste: 'BC',    community: 'Kongu Vellalar',    noOfBrothers: 0,    noOfSisters: 0,    fatherIsLate: false,    motherIsLate: false,    status: 'ACTIVE' as any,    astrology: { mode: 'none' },    residence: '',    ageMin: 21,    ageMax: 40,    heightMinId: 122,    heightMaxId: 231,};export const useProfileForm = () => {    const { formatValue } = useInputFormatting();    const [isDirty, setIsDirty] = useState(false);    const [formData, setFormData] = useState<Partial<Profile>>({ ...DEFAULT_FORM_DATA });    const [fieldErrors, setFieldErrors] = useState<Record<string, string>>({});    const [touchedFields, setTouchedFields] = useState<Set<string>>(new Set());    const [stepErrors, setStepErrors] = useState<Record<number, string[]>>({});    const { data: draftData, isLoaded, hydrate, update } = useIndexedDB();    const formDataRef = useRef(formData);    formDataRef.current = formData;    useEffect(() => {        if (isLoaded && draftData) {            const params = new URLSearchParams(window.location.search);            if (params.has('draft') || params.has('id')) return;            const restored = draftToForm(draftData);            setFormData(prev => ({ ...DEFAULT_FORM_DATA, ...restored }));            setIsDirty(false);        }    }, [isLoaded, draftData]);    const updateField = (field: keyof Profile, value: any) => {        let formattedValue = value;        if (typeof value === 'string' && value.length > 0) {            const excluded = ['dob', 'birthTime', 'mobile', 'status', 'adminVerified', 'primaryUploadId', 'rasiChartUploadId', 'navamsaChartUploadId'];            const titleCaseFields = [                'nameEn', 'fatherNameEn', 'motherNameEn', 'companyName',                 'birthPlace', 'jobLocationEn', 'kuladeivamEn', 'jobDetail',                'fatherJob', 'motherJob', 'community', 'education',                'currentDistrictEn', 'currentCityEn', 'currentStateEn', 'currentCountryEn',                'land', 'otherAssets', 'expectation', 'preferredLocationEn'            ];                        let mode: InputFormattingMode = 'sentence';            if (excluded.includes(field as string)) mode = 'none';            if (field === 'email') mode = 'email';            if (field === 'caste') mode = 'uppercase';            if (titleCaseFields.includes(field as string)) mode = 'title';            formattedValue = formatValue(value, { mode });        }        setFormData(prev => ({ ...prev, [field]: formattedValue }));        setIsDirty(true);    };    const persistDraft = useCallback(async () => {        const current = formDataRef.current;        if (!current) return;        const draft = formToDraft(current as any);        await indexedDBStorage.saveDraft(draft);        update(draft);    }, [update]);    const restoreDraft = useCallback((draftData: any) => {        if (!draftData) return;        const minDobDate = getMinDobDate();        const maxDobDate = getMaxDobDate();        const rawDob = draftData.basic?.dob || draftData.personal?.dob;        const validDob = rawDob && rawDob >= minDobDate && rawDob <= maxDobDate ? rawDob : undefined;        const partnerPref = { ...(draftData.partnerPreference || {}) };        const restored = {            ...DEFAULT_FORM_DATA,            ...draftData.basic,            ...draftData.personal,            ...draftData.community,            ...draftData.professional,            ...draftData.family,            ...draftData.assets,            ...partnerPref,            land: draftData.assets?.landEn ?? null,            otherAssets: draftData.assets?.otherAssetsEn ?? null,            expectation: draftData.partnerPreference?.expectationNoteEn ?? null,            dob: validDob,            astrology: draftData.basic?.astrology || { mode: 'none' },        };        setFormData(restored);        setIsDirty(false);    }, []);    const markTouched = useCallback((field: string) => {        setTouchedFields(prev => {            if (prev.has(field)) return prev;            const next = new Set(prev);            next.add(field);            return next;        });        setTimeout(() => {            const error = validateField(field, formDataRef.current[field as keyof Profile]);            setFieldErrors(prev => {                const next = { ...prev };                if (error) next[field] = translateError(error);                else delete next[field];                return next;            });        }, 0);    }, []);    const validateStepOnNav = useCallback((step: number): string[] => {        const { fieldErrors: errors } = validateStepAtNav(step, formDataRef.current);        const translated: Record<string, string> = {};        for (const [field, msg] of Object.entries(errors)) {            translated[field] = translateError(msg);        }        const errorList = Object.values(translated);        setFieldErrors(prev => ({ ...prev, ...translated }));        if (errorList.length > 0) {            setStepErrors(prev => ({ ...prev, [step]: errorList }));        }        return errorList;    }, []);    const reset = useCallback(() => {        setFormData({ ...DEFAULT_FORM_DATA });        setFieldErrors({});        setTouchedFields(new Set());        setStepErrors({});        setIsDirty(false);    }, []);    return {        formData,        updateField,        setFormData,        reset,        restoreDraft,        persistDraft,        isDirty,        setIsDirty,        formDataRef,        fieldErrors,        touchedFields,        stepErrors,        markTouched,        validateStepOnNav,    };};