import { useState, useCallback } from 'react';import { useQueryClient } from '@tanstack/react-query';import { useAuth } from '@/hooks/useAuth';import { resolveErrorType } from '@/lib/errors';import type { ErrorType } from '@/lib/errors';import type { Profile } from '@/types/profile';import { useProfileQuery } from '@/queries/useProfileQueries';import { useToggleShortlistMutation } from '@/queries/useProfileMutations';import { queryKeys } from '@/queries/queryKeys';import { useMyCapabilitiesQuery } from '@/queries/useMembershipQueries';export type ViewerRole = 'self' | 'admin' | 'public';export interface UseProfileViewResult {  profile: Profile | null;  loading: boolean;  errorType: ErrorType;  errorMessage: string | null;  viewerRole: ViewerRole;  shortlisted: boolean;  handleRetry: () => void;  handleToggleShortlist: () => Promise<void>;}export function useProfileView(profileId: string | undefined): UseProfileViewResult {  const { user } = useAuth();  const qc = useQueryClient();  const [shortlisted, setShortlisted] = useState(false);  const capsQuery = useMyCapabilitiesQuery();  const capsData = capsQuery.data as any;  const caps = capsData?.capabilities;  const openRemaining = caps?.openRemaining ?? -1;  const openLimit = caps?.openLimit ?? -1;  const skipFetch = openRemaining === 0 && !!profileId && openLimit >= 0;  const profileQuery = useProfileQuery(profileId, { enabled: !skipFetch });  const toggleMutation = useToggleShortlistMutation();  const profile: Profile | null = profileQuery.data ?? null;  const loading = profileQuery.isPending && !profileQuery.isFetched;  let errorType: ErrorType = null;  let errorMessage: string | null = null;  if (skipFetch && !profileQuery.isFetched) {    errorType = 'FORBIDDEN';    errorMessage = 'Your daily profile view limit has been reached. Upgrade your plan for more views.';  } else if (profileQuery.error) {    errorType = resolveErrorType(profileQuery.error).type;    errorMessage = resolveErrorType(profileQuery.error).message;  }  const viewerRole: ViewerRole = (() => {    if (!user || !profile) return 'public';    if (profile.isOwner) return 'self';    if (user.roles?.includes('ADMIN')) return 'admin';    return 'public';  })();  const handleRetry = useCallback(() => {    if (profileId) {      qc.invalidateQueries({ queryKey: queryKeys.profile.detail(profileId) });    }  }, [qc, profileId]);  const handleToggleShortlist = useCallback(async () => {    if (!profile) return;    const action = shortlisted ? 'remove' : 'add';    try {      await toggleMutation.mutateAsync({ profileId: profile.id, action });      setShortlisted((prev) => !prev);    } catch {      // optimistic UI: snackbar already shown by mutation onError    }  }, [profile, shortlisted, toggleMutation]);  return {    profile,    loading,    errorType,    errorMessage,    viewerRole,    shortlisted,    handleRetry,    handleToggleShortlist,  };}