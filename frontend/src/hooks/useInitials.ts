import { useTranslation } from 'react-i18next';import { useCallback } from 'react';/** * useInitials Hook *  * WHAT: A centralized hook for extracting initials from names. * WHY: Correctly handles grapheme clusters for Indic languages like Tamil (e.g., 'வே' remains one unit). *  * USAGE: * const { getInitials } = useInitials(); * console.log(getInitials("வேலவன் மணி")); // Outputs "வேம" */export const useInitials = () => {    const { i18n } = useTranslation();    const getInitials = useCallback((name: string | null | undefined, lang?: string) => {        if (!name || name.trim().length === 0) return 'U';        const effectiveLang = lang || i18n.language || 'en';                try {            const words = name.trim().split(/\s+/);                        // Intl.Segmenter provides proper grapheme splitting (essential for Tamil, etc.)            // This ensures we get the "first letter" in the way a human reads it (syllable-based)            const segmenter = new (Intl as any).Segmenter(effectiveLang, { granularity: 'grapheme' });                        const processedChunks = words                .map(word => {                    const segments = Array.from(segmenter.segment(word));                    return segments.length > 0 ? (segments[0] as any).segment : '';                })                .filter(Boolean);                        // Take the first two valid initials/clusters            return processedChunks.slice(0, 2).join('').toUpperCase();        } catch (e) {            // Fallback for very old environments or unexpected errors            // Note: This fallback might cut complex clusters in half, which is why Segmenter is preferred.            return (name || '')                .split(/\s+/)                .filter(Boolean)                .map(n => n[0])                .join('')                .substring(0, 2)                .toUpperCase();        }    }, [i18n.language]);    return { getInitials };};