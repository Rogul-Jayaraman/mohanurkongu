import { useTranslation } from 'react-i18next';/** * Centralized translation hook that ensures consistent usage across all components * This hook provides a unified interface for accessing translations with proper namespacing */export const useTranslations = (namespaces: string | string[] = ['common']) => {    const { t, i18n } = useTranslation(namespaces);        // Helper function to get translation with fallback across all provided namespaces    const translate = (key: string, options?: any): string => {        // If key already contains namespace with ':', use it directly        if (key.includes(':')) {            return t(key, options) as string;        }        // Support dot notation (e.g., 'errors.invalidCredentials')        const nsPrefixes = ['errors.', 'auth.', 'signup.', 'common.', 'dashboard.', 'profile_new.', 'adminLogin.', 'adminLayout.', 'adminMatrimony.', 'adminMandapam.', 'calendar.'];        const matchedPrefix = nsPrefixes.find(p => key.startsWith(p));        if (matchedPrefix) {            const ns = matchedPrefix.slice(0, -1);            const actualKey = key.slice(matchedPrefix.length);            return t(`${ns}:${actualKey}`, options) as string;        }                // Convert namespaces to array if it's a string        const nsList = Array.isArray(namespaces) ? namespaces : [namespaces];        // Try each namespace in order        for (const ns of nsList) {            const namespacedKey = `${ns}:${key}`;                        // i18n.exists() is the most reliable way to check for key presence            if (i18n.exists(namespacedKey)) {                return t(namespacedKey, options) as string;            }        }                // Final fallback to common if not already checked        if (!nsList.includes('common')) {            const commonKey = `common:${key}`;            if (i18n.exists(commonKey)) {                return t(commonKey, options) as string;            }        }                // Final fallback to the key itself using default i18next behavior        return t(key, options) as string;    };        /**     * Helper to translate errors from API or forms     * Uses error code (e.g., AUTH_INVALID_CREDENTIALS) to look up errors: namespace.     * Falls back to message-based mapping for non-code strings.     */    const translateError = (error: any, code?: string): string => {        if (!error) return '';        // If error is an AppError with a code, use it        if (code && i18n.exists(`errors:${code}`)) {            return translate(`errors:${code}`);        }        const errorStr = typeof error === 'string' ? error : (error.code || error.message || error.details || '');        // Try direct key match in errors namespace        if (i18n.exists(`errors:${errorStr}`)) {            return translate(`errors:${errorStr}`);        }        // Map common error strings / backend messages to translation keys        const commonErrors: Record<string, string> = {            'Invalid or expired verification code': 'AUTH_VERIFICATION_EXPIRED',            'Invalid verification code': 'AUTH_OTP_INVALID',            'Verification code has expired': 'AUTH_OTP_EXPIRED',            'Invalid password': 'AUTH_INVALID_CREDENTIALS',            'User already exists': 'AUTH_EMAIL_EXISTS',            'Invalid phone/email or password': 'AUTH_INVALID_CREDENTIALS',            'Login failed. Please check your credentials.': 'AUTH_INVALID_CREDENTIALS',            'Network Error': 'INTERNAL_ERROR',            'Internal server error': 'INTERNAL_ERROR',            'Failed to send verification code. Please try again.': 'AUTH_OTP_COOLDOWN',            'Registration failed. Please try again.': 'VALIDATION_ERROR',            'Failed to reset password. Please try again.': 'AUTH_RESET_SESSION_INVALID',            'Session expired': 'AUTH_SESSION_EXPIRED',            'Request timed out': 'REQUEST_TIMEOUT',        };        const mappedKey = commonErrors[errorStr];        if (mappedKey && i18n.exists(`errors:${mappedKey}`)) {            return translate(`errors:${mappedKey}`);        }        // Last resort: extract error from object or return raw string        if (typeof error === 'object' && error !== null) {            return error.message || error.details || errorStr;        }        return errorStr;    };        return {        t: translate,        translateError,        i18n,        language: i18n.language,        changeLanguage: i18n.changeLanguage    };};