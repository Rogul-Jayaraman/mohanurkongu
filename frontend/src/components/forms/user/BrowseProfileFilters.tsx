import React, { useState, useEffect } from 'react';import { createPortal } from 'react-dom';import { motion, AnimatePresence } from 'framer-motion';import { useTranslation } from 'react-i18next';import { DISTRICTS, TALUKS_BY_DISTRICT, DISTRICT_TAMIL, TALUK_TAMIL } from '@/constants/locations';import {    RASI_OPTIONS,    NAKSHATRA_OPTIONS,    DOSHAM_OPTIONS,    MARITAL_STATUS_OPTIONS,    DIET_OPTIONS,    COMPLEXION_OPTIONS,    JOB_SECTOR_OPTIONS,    RESIDENCE_OPTIONS,    KULAM_OPTIONS} from '@/constants/options';import Dropdown, { type DropdownOption } from '@/components/ui/forms/Dropdown';import MultiSelect from '@/components/ui/forms/MultiSelect';import { Input } from '@/components/ui/forms/Input';import RangeSlider from '@/components/ui/forms/RangeSlider';import { Lock, Search, RotateCcw, X, Star, MapPin, Users, User, GraduationCap, Heart } from 'lucide-react';const ICON_GRADIENTS: Record<string, string> = {    astrology: 'bg-linear-to-br from-rosewood/80 via-dark-rosewood/95 to-rosewood/80 text-white',    location: 'bg-linear-to-br from-ivory to-gold/40 text-rosewood',    community: 'bg-linear-to-br from-rosewood/80 via-dark-rosewood/95 to-rosewood/80 text-white',    physical: 'bg-linear-to-br from-ivory to-gold/40 text-rosewood',    personal: 'bg-linear-to-br from-rosewood/80 via-dark-rosewood/95 to-rosewood/80 text-white',    education: 'bg-linear-to-br from-ivory to-gold/40 text-rosewood',};const SECTION_ICONS: Record<string, React.ReactNode> = {    astrology: <Star size={16} />,    location: <MapPin size={16} />,    community: <Users size={16} />,    physical: <User size={16} />,    personal: <Heart size={16} />,    education: <GraduationCap size={16} />,};interface ActiveFilterChipsProps {    filters: any;    onClear: (key: string) => void;    onClearAll: () => void;}export const ActiveFilterChips: React.FC<ActiveFilterChipsProps> = ({ filters, onClear, onClearAll }) => {    const { i18n } = useTranslation();    const lang = i18n.language as 'en' | 'ta';    const toTitleCase = (str: string) => str.toLowerCase().split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ');    const getChipLabel = (key: string, value: any) => {        if (value === undefined || value === null || value === '') return null;        switch (key) {            case 'currentDistrict':            case 'nativeDistrict':                return { en: toTitleCase(String(value)), ta: DISTRICT_TAMIL[value] || value, category: key === 'currentDistrict' ? 'Current District' : 'Native District' };            case 'currentCity':            case 'currentTaluk':                return { en: toTitleCase(String(value)), ta: TALUK_TAMIL[value] || value, category: 'Taluk' };            case 'kulam': {                const k = KULAM_OPTIONS.find((o: any) => o.value === value);                return { en: k?.label || value, ta: k?.labelTa || value, category: 'Kulam' };            }            case 'rasi': {                const r = RASI_OPTIONS.find((o: any) => o.value === value);                return { en: r?.label || value, ta: r?.labelTa || value, category: 'Rasi' };            }            case 'nakshatra': {                const n = NAKSHATRA_OPTIONS.find((o: any) => o.value === value);                return { en: n?.label || value, ta: n?.labelTa || value, category: 'Star' };            }            case 'laganam': {                const l = RASI_OPTIONS.find((o: any) => o.value === value);                return { en: l?.label || value, ta: l?.labelTa || value, category: 'Lagnam' };            }            case 'dosham': {                const d = DOSHAM_OPTIONS.find((o: any) => o.value === value);                return { en: d?.label || value, ta: d?.labelTa || value, category: 'Dosham' };            }            case 'maritalStatus': {                const m = MARITAL_STATUS_OPTIONS.find((o: any) => o.value === value);                return { en: m?.label || value, ta: m?.labelTa || value, category: 'Status' };            }            case 'diet': {                const d = DIET_OPTIONS.find((o: any) => o.value === value);                return { en: d?.label || value, ta: d?.labelTa || value, category: 'Diet' };            }            case 'complexion': {                const c = COMPLEXION_OPTIONS.find((o: any) => o.value === value);                return { en: c?.label || value, ta: c?.labelTa || value, category: 'Complexion' };            }            case 'residence': {                const r = RESIDENCE_OPTIONS.find((o: any) => o.value === value);                return { en: r?.label || value, ta: r?.labelTa || value, category: 'Residence' };            }            case 'currentSector':            case 'jobSector': {                const j = JOB_SECTOR_OPTIONS.find((o: any) => o.value === value);                return { en: j?.label || value, ta: j?.labelTa || value, category: 'Job Sector' };            }            case 'education':                return { en: toTitleCase(String(value)), ta: value, category: 'Education' };            case 'jobTitle':                return { en: toTitleCase(String(value)), ta: value, category: 'Job Title' };            case 'jobLocation':                return { en: toTitleCase(String(value)), ta: value, category: 'Job Location' };            case 'kuladeivam':                return { en: toTitleCase(String(value)), ta: value, category: 'Kuladeivam' };            case 'minAge':            case 'maxAge':                return { en: `${value} yrs`, ta: `${value} ஆண்டுகள்`, category: key === 'minAge' ? 'Min Age' : 'Max Age' };            case 'minHeight':            case 'maxHeight':                return { en: `${value} cm`, ta: `${value} செ.மீ`, category: key === 'minHeight' ? 'Min Height' : 'Max Height' };            case 'minWeight':            case 'maxWeight':                return { en: `${value} kg`, ta: `${value} கி.கி`, category: key === 'minWeight' ? 'Min Weight' : 'Max Weight' };            case 'minSalary':            case 'maxSalary':                return { en: `₹${value} L`, ta: `₹${value} இலட்சம்`, category: key === 'minSalary' ? 'Min Salary' : 'Max Salary' };            default:                return null;        }    };    const activeKeys = Object.keys(filters).filter(key => {        const val = filters[key];        if (val === undefined || val === null || val === '') return false;        if (Array.isArray(val) && val.length === 0) return false;        if (key === 'sort') return false;        return true;    });    if (activeKeys.length === 0) return null;    return (        <div className="flex flex-wrap items-center gap-2.5 py-4">            {activeKeys.map(key => {                const info = getChipLabel(key, filters[key]);                if (!info) return null;                return (                    <motion.div                        key={key}                        initial={{ opacity: 0, scale: 0.8 }}                        animate={{ opacity: 1, scale: 1 }}                        className="flex items-center gap-2 pl-3 pr-2 py-1.5 bg-white/40 backdrop-blur-md border border-gold/20 rounded-xl shadow-sm hover:border-gold transition-all group"                    >                        <div className="flex flex-col leading-tight">                            <span className="text-[7px] font-black text-rosewood/40 uppercase tracking-widest">{info.category}</span>                            <span className="text-[11px] font-bold text-rosewood">{info[lang === 'ta' ? 'ta' : 'en']}</span>                        </div>                        <button onClick={() => onClear(key)} className="size-6 flex items-center justify-center rounded-lg bg-rosewood/5 hover:bg-rosewood text-rosewood/40 hover:text-white transition-all duration-300">                            <X size={12} />                        </button>                    </motion.div>                );            })}            <button onClick={onClearAll} className="text-[10px] font-black text-rosewood/40 hover:text-rosewood px-4 py-2 rounded-xl border border-dashed border-rosewood/10 hover:border-rosewood/30 hover:bg-rosewood/5 transition-all duration-500 uppercase tracking-widest">                {lang === 'ta' ? 'அனைத்தையும் நீக்கு' : 'Reset Filters'}            </button>        </div>    );};const LockedOverlay: React.FC<{ message: string; messageTa: string; gradientKey: string }> = ({ message, messageTa, gradientKey }) => {    const { i18n } = useTranslation();    const isTamil = i18n.language === 'ta';    return (        <div className="absolute inset-0 z-20 flex items-center justify-center bg-ivory/80 backdrop-blur-[2px] rounded-xl">            <motion.div                initial={{ opacity: 0, y: 10 }}                animate={{ opacity: 1, y: 0 }}                className="flex flex-col items-center gap-3 px-8 py-6 bg-white rounded-2xl shadow-lg border border-gold/20 mx-4"            >                <div className={`size-14 rounded-full flex items-center justify-center border ${ICON_GRADIENTS[gradientKey]} border-gold/10 shadow-sm`}>                    <Lock size={22} />                </div>                <p className="text-xs font-semibold text-rosewood/60 text-center max-w-[220px] leading-relaxed">                    {isTamil ? messageTa : message}                </p>            </motion.div>        </div>    );};interface SectionCardProps {    iconKey: string;    title: string;    children: React.ReactNode;    columns?: 1 | 2 | 3;    spanClassName?: string;    locked?: boolean;    lockedMessage?: string;    lockedMessageTa?: string;}const SectionCard: React.FC<SectionCardProps> = ({    iconKey, title, children, columns = 1, spanClassName = '',    locked = false, lockedMessage = '', lockedMessageTa = ''}) => {    return (        <motion.div            initial={{ opacity: 0, y: 20 }}            animate={{ opacity: 1, y: 0 }}            transition={{ duration: 0.4 }}            className={`bg-ivory rounded-xl shadow-sm border border-gold/20 px-4 sm:px-6 md:px-8 py-4 sm:py-5 md:py-6 h-full relative overflow-hidden hover:shadow-md transition-shadow duration-500 ${spanClassName}`}        >            <div className="flex items-center gap-3 mb-4 border-b border-gold/10 pb-2">                <div className={`p-1.5 sm:p-2.5 rounded-xl shadow-sm shrink-0 ${ICON_GRADIENTS[iconKey]}`}>                    {SECTION_ICONS[iconKey]}                </div>                <h2 className="font-serif text-rosewood font-black flex-1 text-lg sm:text-xl">{title}</h2>            </div>            <div className={`gap-x-6 gap-y-4 ${                columns === 1 ? 'flex flex-col space-y-3' :                columns === 2 ? 'grid grid-cols-1 md:grid-cols-2' :                'grid grid-cols-1 md:grid-cols-3'            }`}>                {children}                {locked && <LockedOverlay message={lockedMessage} messageTa={lockedMessageTa} gradientKey={iconKey} />}            </div>        </motion.div>    );};const FilterDropdown = ({ label, options, value, onChange, placeholder, bilingual }: {    label: string; options: DropdownOption[]; value: string; onChange: (v: string) => void; placeholder?: string; bilingual?: boolean}) => (    <div className="space-y-1.5">        <label className="block text-[11px] sm:text-xs font-bold text-rosewood tracking-tight ml-1">{label}</label>        <Dropdown            options={options}            value={value}            onChange={onChange}            placeholder={placeholder || ''}            searchable={options.length > 8}            bilingual={bilingual}            className="w-full"            menuClassName="min-w-[280px]"        />    </div>);interface BrowseProfileFiltersProps {    isOpen: boolean;    onClose: () => void;    filters: any;    setFilters: (filters: any) => void;    onApply: () => void;    searchLevel?: string;}export const BrowseProfileFilters: React.FC<BrowseProfileFiltersProps> = ({ isOpen, onClose, filters, setFilters, onApply, searchLevel = 'BASIC' }) => {    const { t, i18n } = useTranslation(['dashboard', 'common', 'browse']);    const [localFilters, setLocalFilters] = useState<any>({});    useEffect(() => {        if (isOpen) {            setLocalFilters(filters || {});            document.body.style.overflow = 'hidden';        }        return () => { document.body.style.overflow = ''; };    }, [isOpen, filters]);    const handleChange = (key: string, value: any) => {        setLocalFilters((prev: any) => {            const updated = { ...prev, [key]: value };            if (value === '' || value === undefined || value === null || (Array.isArray(value) && value.length === 0)) {                delete updated[key];            }            return updated;        });    };    const handleApply = () => { setFilters(localFilters); onApply(); };    const handleClearAll = () => { setLocalFilters({}); };    const handleReset = () => { setLocalFilters({}); setFilters({}); onApply(); };    const levels = ['BASIC', 'EXTENDED', 'ADVANCED', 'FULL'];    const isAtLeast = (min: string) => levels.indexOf(searchLevel) >= levels.indexOf(min);    const astrologyLocked = !isAtLeast('ADVANCED');    const educationLocked = !isAtLeast('EXTENDED');    const residenceLocked = !isAtLeast('FULL');    const activeCount = Object.keys(localFilters).filter(k => {        const v = localFilters[k];        return v !== '' && v !== undefined && v !== null && v !== false;    }).length;    const toTitleCase = (str: string) => str.toLowerCase().split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ');    const districtOptions: DropdownOption[] = DISTRICTS.map(d => ({        value: d, label: { en: toTitleCase(d), ta: (DISTRICT_TAMIL as any)[d] || d }    }));    const getTalukOptions = (district: string): DropdownOption[] =>        ((TALUKS_BY_DISTRICT as any)[district] || []).map((t: string) => ({            value: t, label: { en: toTitleCase(t), ta: (TALUK_TAMIL as any)[t] || t }        }));    const rasiOpts: DropdownOption[] = RASI_OPTIONS.map(r => ({ value: r.value, label: { en: r.label, ta: r.labelTa } }));    const nakshatraOpts: DropdownOption[] = NAKSHATRA_OPTIONS.map(n => ({ value: n.value, label: { en: n.label, ta: n.labelTa } }));    const doshamOpts: DropdownOption[] = DOSHAM_OPTIONS.map(d => ({ value: d.value, label: { en: d.label, ta: d.labelTa } }));    const complexionOpts: DropdownOption[] = COMPLEXION_OPTIONS.map(c => ({ value: c.value, label: { en: c.label, ta: c.labelTa } }));    const dietOpts: DropdownOption[] = DIET_OPTIONS.map(d => ({ value: d.value, label: { en: d.label, ta: d.labelTa } }));    const maritalStatusOpts: DropdownOption[] = MARITAL_STATUS_OPTIONS.map(m => ({ value: m.value, label: { en: m.label, ta: m.labelTa } }));    const jobSectorOpts: DropdownOption[] = JOB_SECTOR_OPTIONS.map(j => ({ value: j.value, label: { en: j.label, ta: j.labelTa } }));    const residenceOpts: DropdownOption[] = RESIDENCE_OPTIONS.map(r => ({ value: r.value, label: { en: r.label, ta: r.labelTa } }));    const kulamMultiOpts = KULAM_OPTIONS.map((k: any) => {        const labelEn = typeof k.label === 'string' ? k.label : k.label.en;        const labelTa = typeof k.label === 'string' ? k.label : (k.label.ta || '');        return { value: k.value, label: { en: labelEn, ta: labelTa } };    });    const kulamDropdownOpts: DropdownOption[] = kulamMultiOpts.map((k: any) => ({        value: k.value, label: k.label    }));    return createPortal(        <AnimatePresence>            {isOpen && (                <div className="fixed inset-0 z-[9999] flex items-center justify-center overflow-hidden filter-portal">                    <motion.div                        initial={{ opacity: 0 }}                        animate={{ opacity: 1 }}                        exit={{ opacity: 0 }}                        className="absolute inset-0 bg-ivory"                    />                    <motion.div                        initial={{ y: '100%' }}                        animate={{ y: 0 }}                        exit={{ y: '100%' }}                        transition={{ type: 'spring', damping: 28, stiffness: 280, mass: 0.8 }}                        className="relative w-full h-full bg-ivory flex flex-col overflow-hidden kolam-bg"                    >                        <div className="absolute top-0 right-0 w-[400px] h-[400px] bg-gold/5 rounded-full blur-[120px] pointer-events-none -translate-y-1/2 translate-x-1/4" />                        <div className="absolute bottom-0 left-0 w-[300px] h-[300px] bg-rosewood/5 rounded-full blur-[100px] pointer-events-none translate-y-1/3 -translate-x-1/4" />                        <div className="shrink-0 relative z-100 px-6 lg:px-12 py-5 bg-white/80 backdrop-blur-2xl border-b border-gold/10 flex items-center justify-between shadow-sm">                            <div className="flex items-center gap-5">                                <div className="size-11 rounded-xl bg-linear-to-br from-rosewood/80 via-dark-rosewood/95 to-rosewood/80 flex items-center justify-center shadow-lg shadow-rosewood/20 relative overflow-hidden">                                    <span className="material-symbols-outlined text-white text-2xl! relative z-10">tune</span>                                </div>                                <div className="flex flex-col">                                    <h2 className="text-xl font-black text-rosewood tracking-tight leading-none italic">{t('browse:advanced_filters')}</h2>                                    <span className="text-[10px] font-bold text-rosewood/40 uppercase tracking-[0.25em] mt-2">{t('browse:filters_subtitle')}</span>                                </div>                            </div>                            <div className="flex items-center gap-3">                                {activeCount > 0 && (                                    <span className="px-2.5 py-1 rounded-full bg-rosewood/10 text-rosewood text-[10px] font-black tabular-nums border border-rosewood/20">                                        {activeCount}                                    </span>                                )}                                <button onClick={onClose} className="size-11 flex items-center justify-center rounded-xl bg-white text-rosewood shadow-sm border border-gold/20 hover:bg-linear-to-br hover:from-rosewood/80 hover:via-dark-rosewood/95 hover:to-rosewood/80 hover:text-white transition-all duration-300 group" aria-label="Close filters">                                    <span className="material-symbols-outlined text-2xl! group-hover:rotate-90 transition-transform duration-500">close</span>                                </button>                            </div>                        </div>                        <div className="flex-1 overflow-y-auto filter-scroll-container custom-scrollbar relative z-10 bg-slate-50/30">                            <div className="max-w-7xl mx-auto px-6 lg:px-12 py-12 grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-12">                                <SectionCard                                    iconKey="location"                                    title={t('browse:section_location')}                                    columns={1}                                >                                    <FilterDropdown label={t('browse:current_district')} options={districtOptions} value={localFilters.currentDistrict || ''} onChange={v => { handleChange('currentDistrict', v); handleChange('currentCity', ''); }} />                                    <FilterDropdown label={t('browse:city_taluk')} options={localFilters.currentDistrict ? getTalukOptions(localFilters.currentDistrict) : []} value={localFilters.currentCity || ''} onChange={v => handleChange('currentCity', v)} placeholder={t('browse:select_taluk', 'Select Taluk')} />                                    <div className="relative">                                        {residenceLocked && (                                            <div className="absolute inset-0 z-10 flex items-center justify-center bg-white/60 backdrop-blur-[1px] rounded-lg">                                                <div className="flex flex-col items-center gap-2">                                                    <Lock size={14} className="text-rosewood/30" />                                                    <span className="text-[9px] font-bold text-rosewood/40 text-center">                                                        {i18n.language === 'ta' ? 'பிளாட்டினத்தில் மட்டும்' : 'Available in Platinum'}                                                    </span>                                                </div>                                            </div>                                        )}                                        <FilterDropdown label={t('browse:residence')} options={residenceOpts} value={localFilters.residence || ''} onChange={v => handleChange('residence', v)} />                                    </div>                                </SectionCard>                                <SectionCard                                    iconKey="community"                                    title={t('browse:section_community')}                                    columns={2}                                >                                    <FilterDropdown label={t('browse:kulam')} options={kulamDropdownOpts} value={localFilters.kulam || ''} onChange={v => handleChange('kulam', v)} />                                    <Input label={t('browse:kuladeivam')} icon="temple_hindu" name="kuladeivam" placeholder={t('browse:type_name_placeholder', 'Type name...')} value={localFilters.kuladeivam || ''} onChange={(e: React.ChangeEvent<HTMLInputElement>) => handleChange('kuladeivam', e.target.value)} className="" />                                    <div className="md:col-span-2">                                        <MultiSelect options={kulamMultiOpts} values={localFilters.kulamAvoid || []} onChange={(vals: string[]) => handleChange('kulamAvoid', vals)} label={t('browse:kulam_avoid')} placeholder={t('browse:select_kulam_avoid', 'Select Kulam to avoid...')} />                                    </div>                                </SectionCard>                                <SectionCard                                    iconKey="physical"                                    title={t('browse:section_physical')}                                    columns={1}                                >                                    <RangeSlider min={21} max={40} value={[Number(localFilters.minAge) || 21, Number(localFilters.maxAge) || 40]} onChange={(val: number[]) => { handleChange('minAge', val[0]); handleChange('maxAge', val[1]); }} label={t('browse:age_range')} />                                    <RangeSlider min={140} max={210} unit="cm" value={[Number(localFilters.minHeight) || 140, Number(localFilters.maxHeight) || 210]} onChange={(val: number[]) => { handleChange('minHeight', val[0]); handleChange('maxHeight', val[1]); }} label={t('browse:height_range')} />                                    <RangeSlider min={30} max={150} unit="kg" value={[Number(localFilters.minWeight) || 30, Number(localFilters.maxWeight) || 150]} onChange={(val: number[]) => { handleChange('minWeight', val[0]); handleChange('maxWeight', val[1]); }} label={t('browse:weight_range')} />                                </SectionCard>                                <SectionCard                                    iconKey="personal"                                    title={t('browse:section_personal')}                                    columns={1}                                >                                    <FilterDropdown label={t('browse:marital_status')} options={maritalStatusOpts} value={localFilters.maritalStatus || ''} onChange={v => handleChange('maritalStatus', v)} />                                    <FilterDropdown label={t('browse:complexion')} options={complexionOpts} value={localFilters.complexion || ''} onChange={v => handleChange('complexion', v)} />                                    <FilterDropdown label={t('browse:diet')} options={dietOpts} value={localFilters.diet || ''} onChange={v => handleChange('diet', v)} />                                </SectionCard>                                <SectionCard                                    iconKey="education"                                    title={t('browse:section_education')}                                    columns={2}                                    spanClassName="md:col-span-2"                                    locked={educationLocked}                                    lockedMessage={t('browse:filters.locked.education', 'Upgrade to Silver to access education & career filters')}                                    lockedMessageTa="கல்வி மற்றும் பணி வடிப்பான்களை அணுக வெள்ளித் திட்டத்திற்கு மேம்படுத்தவும்"                                >                                    <Input label={t('browse:education')} icon="school" name="education" placeholder={t('browse:education_placeholder')} value={localFilters.education || ''} onChange={(e: React.ChangeEvent<HTMLInputElement>) => handleChange('education', e.target.value)} className="h-14" />                                    <Input label={t('browse:job_title')} icon="badge" name="jobTitle" placeholder={t('browse:job_title_placeholder')} value={localFilters.jobTitle || ''} onChange={(e: React.ChangeEvent<HTMLInputElement>) => handleChange('jobTitle', e.target.value)} className="h-14" />                                    <Input label={t('browse:job_location')} icon="location_on" name="jobLocation" placeholder={t('browse:job_location_placeholder')} value={localFilters.jobLocation || ''} onChange={(e: React.ChangeEvent<HTMLInputElement>) => handleChange('jobLocation', e.target.value)} className="h-14" />                                    <MultiSelect options={jobSectorOpts} values={localFilters.jobSector || []} onChange={(vals: string[]) => handleChange('jobSector', vals)} label={t('browse:job_sector')} placeholder={t('browse:job_sector')} />                                    <div className="md:col-span-2">                                        <RangeSlider min={1} max={100} unit=" L" value={[Number(localFilters.minSalary) || 1, Number(localFilters.maxSalary) || 100]} onChange={(val: number[]) => { handleChange('minSalary', val[0]); handleChange('maxSalary', val[1]); }} label={t('browse:annual_salary')} />                                    </div>                                </SectionCard>                                <SectionCard                                    iconKey="astrology"                                    title={t('browse:section_astrology')}                                    columns={1}                                    spanClassName="md:col-span-2"                                    locked={astrologyLocked}                                    lockedMessage={t('browse:filters.locked.astrology', 'Upgrade to Gold to access horoscope filters')}                                    lockedMessageTa="ஜாதக வடிப்பான்களை அணுக தங்கத் திட்டத்திற்கு மேம்படுத்தவும்"                                >                                    <FilterDropdown label={t('browse:rasi')} bilingual options={rasiOpts} value={localFilters.rasi || ''} onChange={v => handleChange('rasi', v)} />                                    <FilterDropdown label={t('browse:nakshatra')} bilingual options={nakshatraOpts} value={localFilters.nakshatra || ''} onChange={v => handleChange('nakshatra', v)} />                                    <FilterDropdown label={t('browse:laganam')} bilingual options={rasiOpts} value={localFilters.laganam || ''} onChange={v => handleChange('laganam', v)} />                                    <FilterDropdown label={t('browse:dosham')} options={doshamOpts} value={localFilters.dosham || ''} onChange={v => handleChange('dosham', v)} />                                </SectionCard>                            </div>                        </div>                        <div className="shrink-0 relative z-50 p-6 bg-white/80 backdrop-blur-3xl border-t border-gold/10 flex flex-row gap-4 items-center">                            <button type="button" onClick={handleReset} className="flex-1 h-14 rounded-xl font-black text-[10px] text-rosewood/50 border-2 border-rosewood/10 hover:border-rosewood/30 hover:text-rosewood hover:bg-rosewood/5 transition-all flex items-center justify-center gap-3 group">                                <RotateCcw size={16} className="group-hover:-rotate-45 transition-transform duration-500" />                                {t('browse:reset_filters')}                            </button>                            <button type="button" onClick={handleApply} className="flex-1 h-14 bg-linear-to-br from-rosewood/80 via-dark-rosewood/95 to-rosewood/80 text-white rounded-xl font-black text-[10px] shadow-lg shadow-rosewood/20 hover:shadow-rosewood/30 hover:-translate-y-1 active:translate-y-0 transition-all flex items-center justify-center gap-3 relative overflow-hidden group">                                <div className="absolute inset-0 bg-linear-to-r from-transparent via-white/15 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-700" />                                <Search size={16} className="relative z-10" />                                <span className="relative z-10">{t('browse:show_results')}</span>                            </button>                        </div>                        <style dangerouslySetInnerHTML={{ __html: `                            .custom-scrollbar::-webkit-scrollbar { width: 4px; }                            .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }                            .custom-scrollbar::-webkit-scrollbar-thumb { background: rgba(184, 134, 11, 0.15); border-radius: 10px; }                            .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: rgba(184, 134, 11, 0.3); }                        `}} />                    </motion.div>                </div>            )}        </AnimatePresence>,        document.body    );};