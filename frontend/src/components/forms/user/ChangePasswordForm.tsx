import React, { useState, useEffect } from 'react';import { Shield, Check } from 'lucide-react';import { toast } from 'sonner';import { useLanguage } from '@/context/LanguageContext';import { PasswordField } from '@/components/ui/forms/PasswordField';import { ModalShell } from '@/components/ui/modals/ModalShell';import { useChangePasswordMutation } from '@/queries/useAuthMutations';interface ChangePasswordFormProps {    isOpen: boolean;    onClose: () => void;}export const ChangePasswordForm: React.FC<ChangePasswordFormProps> = ({ isOpen, onClose }) => {    const { t, translateError } = useLanguage();    const changePasswordMutation = useChangePasswordMutation();    const [formData, setFormData] = useState({ oldPassword: '', newPassword: '', confirmPassword: '' });    const isLoading = changePasswordMutation.isPending;    const [strength, setStrength] = useState({ score: 0, label: 'Very Weak', color: 'bg-gray-200' });    useEffect(() => {        const pwd = formData.newPassword;        let s = 0;        if (pwd.length > 8) s++;        if (/[A-Z]/.test(pwd)) s++;        if (/[a-z]/.test(pwd)) s++;        if (/[0-9]/.test(pwd)) s++;        const levels = [            { label: t('myaccount:drawers.change_password.strength.levels.very_weak'), color: 'bg-red-500' },            { label: t('myaccount:drawers.change_password.strength.levels.weak'), color: 'bg-orange-500' },            { label: t('myaccount:drawers.change_password.strength.levels.fair'), color: 'bg-yellow-500' },            { label: t('myaccount:drawers.change_password.strength.levels.good'), color: 'bg-blue-500' },            { label: t('myaccount:drawers.change_password.strength.levels.strong'), color: 'bg-green-500' },            { label: t('myaccount:drawers.change_password.strength.levels.unbreakable'), color: 'bg-emerald-500' }        ];        setStrength({ score: s, label: levels[s]?.label || levels[0].label, color: levels[s]?.color || levels[0].color });    }, [formData.newPassword, t]);    useEffect(() => {        if (!isOpen) {            setFormData({ oldPassword: '', newPassword: '', confirmPassword: '' });        }    }, [isOpen]);    const handleSubmit = async () => {        if (formData.newPassword !== formData.confirmPassword) {            toast.error(t('myaccount:drawers.change_password.errors.mismatch'));            return;        }        if (strength.score < 3) {            toast.warning(t('myaccount:drawers.change_password.errors.too_weak'));            return;        }        try {            await changePasswordMutation.mutateAsync({                currentPassword: formData.oldPassword,                newPassword: formData.newPassword,            });            toast.success(t('myaccount:drawers.change_password.success'));            onClose();        } catch (error: any) {            toast.error(translateError(error) || t('myaccount:drawers.change_password.errors.failed'));        }    };    const submitButton = (        <button            onClick={handleSubmit}            disabled={isLoading || strength.score < 3 || formData.newPassword !== formData.confirmPassword}            className="w-full py-4 bg-rosewood text-white rounded-2xl font-black text-xs uppercase tracking-[0.2em] shadow-xl shadow-rosewood/20 hover:scale-[1.02] active:scale-95 transition-all disabled:bg-sage/20 disabled:text-rosewood/20 disabled:shadow-none disabled:hover:scale-100 disabled:active:scale-100 flex items-center justify-center gap-3"        >            {isLoading ? (                <div className="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" />            ) : (                <>                    <Shield size={16} />                    {t('drawers.change_password.submit')}                </>            )}        </button>    );    return (        <ModalShell            isOpen={isOpen}            onClose={onClose}            icon={<Shield size={24} className="text-rosewood" />}            title={t('drawers.change_password.title')}            size="xl"            footer={submitButton}        >            <div className="space-y-6 max-w-xl mx-auto">                <p className="text-[10px] font-bold text-gray-400 uppercase tracking-widest -mt-2">                    {t('drawers.change_password.subtitle')}                </p>                <PasswordField                    label={t('drawers.change_password.fields.old_password')}                    name="oldPassword"                    icon="lock"                    required                    value={formData.oldPassword}                    onChange={(e) => setFormData({ ...formData, oldPassword: e.target.value })}                    placeholder={t('drawers.change_password.fields.placeholders.old')}                />                <div className="space-y-3">                    <PasswordField                        label={t('drawers.change_password.fields.new_password')}                        name="newPassword"                        icon="lock_open"                        required                        value={formData.newPassword}                        onChange={(e) => setFormData({ ...formData, newPassword: e.target.value })}                        placeholder={t('drawers.change_password.fields.placeholders.new')}                    />                    {formData.newPassword && (                        <div className="bg-white/50 backdrop-blur-sm border border-gold/10 rounded-2xl p-4 mt-2">                            <div className="flex justify-between items-center mb-3">                                <span className="text-[10px] font-black text-rosewood/60 uppercase tracking-widest">                                    {t('drawers.change_password.strength.label', { label: strength.label })}                                </span>                                <span className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest">                                    {t('drawers.change_password.strength.score', { percent: Math.round((strength.score / 4) * 100) })}                                </span>                            </div>                            <div className="flex gap-1.5 h-1.5 mb-4">                                {[1, 2, 3, 4].map((step) => (                                    <div key={step} className={`flex-1 rounded-full transition-all duration-500 ${strength.score >= step ? strength.color : 'bg-gray-100'}`} />                                ))}                            </div>                            <div className="grid grid-cols-2 gap-y-2.5 gap-x-4">                                {[                                    { label: t('drawers.change_password.strength.requirements.length'), met: formData.newPassword.length >= 8 },                                    { label: t('drawers.change_password.strength.requirements.uppercase'), met: /[A-Z]/.test(formData.newPassword) },                                    { label: t('drawers.change_password.strength.requirements.number'), met: /[0-9]/.test(formData.newPassword) },                                ].map(req => (                                    <div key={req.label} className="flex items-center gap-2">                                        <div className={`w-3.5 h-3.5 rounded-full flex items-center justify-center transition-colors ${req.met ? 'bg-green-500' : 'bg-gray-100'}`}>                                            {req.met && <Check size={8} className="text-white" strokeWidth={5} />}                                        </div>                                        <span className={`text-[10px] font-bold tracking-tight ${req.met ? 'text-green-600' : 'text-gray-400'}`}>{req.label}</span>                                    </div>                                ))}                            </div>                        </div>                    )}                </div>                <PasswordField                    label={t('drawers.change_password.fields.confirm_password')}                    name="confirmPassword"                    icon="verified_user"                    required                    value={formData.confirmPassword}                    onChange={(e) => setFormData({ ...formData, confirmPassword: e.target.value })}                    placeholder={t('drawers.change_password.fields.placeholders.confirm')}                    error={formData.confirmPassword && formData.newPassword !== formData.confirmPassword ? t('drawers.change_password.errors.mismatch') : undefined}                />            </div>        </ModalShell>    );};