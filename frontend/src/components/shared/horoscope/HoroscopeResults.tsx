import React from 'react';import { useTranslation } from 'react-i18next';import type { HoroscopeResult } from '@/types/horoscope';import PlanetTable from './PlanetTable';import D1Chart from './D1Chart';import D9Chart from './D9Chart';import CelestialChartsSection from './CelestialChartsSection';interface HoroscopeResultsProps {  result: HoroscopeResult | null;  loading: boolean;  error: string | null;  language?: 'en' | 'ta';  onRegenerate?: () => void;}export default function HoroscopeResults({ result, loading, error, language, onRegenerate }: HoroscopeResultsProps) {  const { t, i18n } = useTranslation(['profile_new']);  const lang = language || i18n.language?.slice(0, 2) as 'en' | 'ta' || 'en';  if (loading) {    return (      <div className="horoscope-loading">        <div className="horoscope-loading-spinner" />      </div>    );  }  if (error) {    return (      <div className="horoscope-error">{error}</div>    );  }  if (!result) return null;  const formatDate = (dateStr: string) => {    try {      return new Date(dateStr).toLocaleDateString(lang === 'ta' ? 'ta-IN' : 'en-IN', {        day: 'numeric', month: 'long', year: 'numeric'      });    } catch {      return dateStr;    }  };  return (    <div className="space-y-8">      <section className="horoscope-section">        <div className="bg-ivory border border-gold/20 rounded-xl shadow-sm transition-all hover:shadow-md">          <div className="bg-ivory/50 px-6 py-4 border-b border-gold-soft rounded-t-xl flex items-center gap-3">            <div className="size-8 bg-rosewood-gradient text-white rounded-xl shadow-sm flex items-center justify-center text-rosewood">              <span className="material-symbols-outlined text-base!">calendar_month</span>            </div>            <h3 className="text-sm font-serif font-bold text-rosewood">              {lang === 'ta' ? 'பிறந்த தின விவரங்கள்' : 'Birth Details'}            </h3>          </div>          <div className="p-6">            <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">              <div>                <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest">{t('profile_new:dob')}</p>                <p className="text-sm font-bold text-slate-700 leading-relaxed">{formatDate(result.input?.dateOfBirth || '')}</p>              </div>              <div>                <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest">{t('profile_new:horoscope.birth_time')}</p>                <p className="text-sm font-bold text-slate-700 leading-relaxed">{result.input?.timeOfBirth || ''}</p>              </div>              <div>                <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest">{t('profile_new:horoscope.birth_place')}</p>                <p className="text-sm font-bold text-slate-700 leading-relaxed">{result.summary?.locationName || ''}</p>              </div>            </div>          </div>        </div>      </section>      <section className="horoscope-section">        <CelestialChartsSection result={result} language={lang} />      </section>      <section className="horoscope-section">        <div className="horoscope-section-header">          <h3 className="horoscope-section-title">{lang === 'ta' ? 'கிரக நிலைகள்' : 'Planet Positions'}</h3>        </div>        <PlanetTable planets={result.planets} language={lang} />      </section>      <section className="horoscope-section">        <div className="horoscope-charts-grid">          <D1Chart lagnaSign={result.summary.lagnaSignIndex} planets={result.planets} title={t('horoscope.rasi_chart')} language={lang} />          <D9Chart planets={result.planets} lagnaNavamsaSignIndex={result.lagnaNavamsa.signIndex} title={t('horoscope.navamsa_chart')} language={lang} />        </div>      </section>      {onRegenerate && (        <div className="flex justify-center pt-2">          <button type="button" onClick={onRegenerate}            className="flex items-center justify-center gap-2 px-6 py-3 bg-ivory border border-gold-soft/30 rounded-xl text-sm font-bold text-rosewood/70 hover:text-rosewood hover:border-rosewood/40 transition-all">            <span className="material-symbols-outlined text-base">cached</span>            <span>{t('profile_new:horoscope.regenerate')}</span>          </button>        </div>      )}    </div>  );}