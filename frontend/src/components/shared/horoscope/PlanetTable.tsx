import React from 'react';import type { PlanetData } from '@/types/horoscope';import { SIGNS, SIGNS_TAMIL, NAKSHATRAS, NAKSHATRAS_TAMIL, PLANETS_TAMIL } from '@/types/horoscope';interface PlanetTableProps {  planets: PlanetData[];  language?: 'en' | 'ta';}const ELEMENT_COLORS: Record<string, string> = {  fire: '#FF6B6B',  earth: '#4ECDC4',  water: '#45B7D1',  air: '#96CEB4',  shadow: '#DDA0DD',};const SIGN_ELEMENTS = [  'fire', 'earth', 'air', 'water',  'fire', 'earth', 'air', 'water',  'fire', 'earth', 'air', 'water',];const SIGNS_SHORT = ['Ar', 'Ta', 'Ge', 'Cn', 'Le', 'Vi', 'Li', 'Sc', 'Sg', 'Cp', 'Aq', 'Pi'];const PLANET_ELEMENTS: Record<string, string> = {  Sun: 'fire', Moon: 'water', Mars: 'fire', Mercury: 'earth',  Jupiter: 'water', Venus: 'water', Saturn: 'air', Rahu: 'shadow', Ketu: 'shadow',};function ZodiacStrip({ planets, language }: { planets: PlanetData[]; language: 'en' | 'ta' }) {  const planetMap: Record<number, string[]> = {};  for (const p of planets) {    if (!planetMap[p.signIndex]) planetMap[p.signIndex] = [];    planetMap[p.signIndex].push(p.name);  }  return (    <div className="horoscope-zodiac-strip">      <div className="flex rounded-lg overflow-hidden h-8">        {Array.from({ length: 12 }, (_, i) => {          const el = SIGN_ELEMENTS[i];          const hasPlanets = planetMap[i]?.length > 0;          const planetNames = planetMap[i] || [];          return (            <div              key={i}              className="horoscope-zodiac-segment"              style={{ backgroundColor: `${ELEMENT_COLORS[el]}15` }}              title={`${SIGNS_SHORT[i]}${hasPlanets ? ': ' + planetNames.join(', ') : ''}`}            >              <span className="leading-none" style={{ color: '#6B7280' }}>                {SIGNS_SHORT[i]}              </span>              {hasPlanets && (                <div className="horoscope-zodiac-dots">                  {planetNames.map((name, idx) => (                    <span                      key={idx}                      className="horoscope-zodiac-dot"                      style={{ backgroundColor: ELEMENT_COLORS[PLANET_ELEMENTS[name] || 'shadow'] }}                    />                  ))}                </div>              )}            </div>          );        })}      </div>      <div className="horoscope-zodiac-legend">        {Object.entries(ELEMENT_COLORS).map(([el, color]) => (          <span key={el} className="horoscope-zodiac-legend-item">            <span className="horoscope-zodiac-legend-dot" style={{ backgroundColor: color }} />            {el.charAt(0).toUpperCase() + el.slice(1)}          </span>        ))}      </div>    </div>  );}function getRowColor(element: string): string {  const colors: Record<string, string> = {    fire: 'rgba(255, 107, 107, 0.04)',    earth: 'rgba(78, 205, 196, 0.04)',    water: 'rgba(69, 183, 209, 0.04)',    air: 'rgba(150, 206, 180, 0.04)',    shadow: 'rgba(221, 160, 221, 0.04)',  };  return colors[element] || 'transparent';}function getRowBorderClass(element: string): string {  const map: Record<string, string> = {    fire: 'horoscope-planet-row--fire',    earth: 'horoscope-planet-row--earth',    water: 'horoscope-planet-row--water',    air: 'horoscope-planet-row--air',    shadow: 'horoscope-planet-row--shadow',  };  return map[element] || '';}export default function PlanetTable({ planets, language = 'en' }: PlanetTableProps) {  const [expandedRow, setExpandedRow] = React.useState<string | null>(null);  const isTa = language === 'ta';  const toggleRow = (name: string) => {    setExpandedRow(expandedRow === name ? null : name);  };  return (    <div className="horoscope-table-wrapper">      <ZodiacStrip planets={planets} language={language} />      <div className="overflow-x-auto">        <table className="horoscope-table" role="table">          <thead>            <tr className="border-b" style={{ borderColor: 'rgba(212, 175, 55, 0.1)' }}>              <th className="horoscope-table-header">{isTa ? 'கிரகம்' : 'Planet'}</th>              <th className="horoscope-table-header">{isTa ? 'ராசி' : 'Sign'}</th>              <th className="horoscope-table-header horoscope-table-header--right">{isTa ? 'பாகை' : 'Degree'}</th>              <th className="horoscope-table-header horoscope-table-header--center">{isTa ? 'வீடு' : 'House'}</th>              <th className="horoscope-table-header horoscope-nakshatra-cell" style={{ display: 'none', padding: '0.75rem' }}>{isTa ? 'நட்சத்திரம்' : 'Nakshatra'}</th>              <th className="horoscope-table-header horoscope-table-header--center horoscope-pada-cell" style={{ display: 'none', padding: '0.75rem' }}>{isTa ? 'பாதம்' : 'Pada'}</th>            </tr>          </thead>          <tbody>            {planets.map((p, i) => {              const element = PLANET_ELEMENTS[p.name] || 'shadow';              const isExpanded = expandedRow === p.name;              const signName = SIGNS[p.signIndex];              const signLabel = isTa ? SIGNS_TAMIL[signName] : signName;              const nakshatraName = NAKSHATRAS[p.nakshatraIndex];              const nakshatraLabel = isTa ? NAKSHATRAS_TAMIL[nakshatraName] : nakshatraName;              return (                <React.Fragment key={p.name}>                  <tr                    onClick={() => toggleRow(p.name)}                    className={`horoscope-planet-row ${getRowBorderClass(element)}`}                    style={{ backgroundColor: getRowColor(element) }}                    tabIndex={0}                    onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') toggleRow(p.name); }}                    role="button"                    aria-expanded={isExpanded}                    aria-label={`${p.name} details`}                  >                    <td className="horoscope-planet-cell">                      <div className="horoscope-planet-item">                        <span className="horoscope-planet-color-dot" style={{ backgroundColor: ELEMENT_COLORS[element] }} />                        <div className={isTa ? 'horoscope-planet-name--ta' : 'horoscope-planet-name--en'}>                          {isTa ? PLANETS_TAMIL[p.name] : p.name}                        </div>                      </div>                    </td>                    <td className="horoscope-planet-cell">                      <div className={isTa ? 'horoscope-planet-name--ta' : 'horoscope-planet-name--en'}>                        {signLabel}                      </div>                    </td>                    <td className="horoscope-degree-cell">{p.degree.toFixed(2)}°</td>                    <td className="horoscope-planet-cell" style={{ textAlign: 'center' }}>                      <span className="horoscope-house-badge">{p.house}</span>                    </td>                    <td className="horoscope-nakshatra-cell">                      <div className={isTa ? 'horoscope-nakshatra-name--ta' : 'horoscope-nakshatra-name--en'}>                        {nakshatraLabel}                      </div>                    </td>                    <td className="horoscope-pada-cell">                      <span className="tabular-nums font-medium" style={{ color: '#3A2F2F' }}>{p.pada}</span>                    </td>                  </tr>                  {isExpanded && (                    <tr className="horoscope-detail-row">                      <td colSpan={6} className="horoscope-detail-grid">                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'தீர்க்க ரேகை' : 'Longitude'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{p.longitude.toFixed(4)}°</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'ராசி' : 'Sign'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{signLabel}</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'வீடு' : 'House'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{isTa ? `வீடு ${p.house}` : `House ${p.house}`}</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'தனிமம்' : 'Element'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{element.charAt(0).toUpperCase() + element.slice(1)}</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'நட்சத்திரம்' : 'Nakshatra'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{nakshatraLabel}</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'பாதம்' : 'Pada'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{String(p.pada)}</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'ராசி பாகை' : 'Degree in Sign'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{p.degree.toFixed(2)}°</p>                        </div>                        <div className="horoscope-detail-item">                          <p className="horoscope-detail-label">{isTa ? 'நவாம்ச ராசி' : 'Navamsa Sign'}</p>                          <p className={isTa ? 'horoscope-detail-value--ta' : 'horoscope-detail-value--en'}>{p.navamsaSignIndex + 1}</p>                        </div>                      </td>                    </tr>                  )}                </React.Fragment>              );            })}          </tbody>        </table>      </div>    </div>  );}