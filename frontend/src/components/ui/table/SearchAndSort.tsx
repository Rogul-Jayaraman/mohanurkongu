import React from 'react';import { useLanguage } from '@/context/LanguageContext';import { motion, AnimatePresence } from 'framer-motion';import { Search, X, SlidersHorizontal, ArrowUpDown, ArrowUp, ArrowDown, RotateCcw } from 'lucide-react';interface SearchAndSortProps {    searchQuery: string;    onSearchChange: (q: string) => void;    dateSort: 'asc' | 'desc' | null;    nameSort?: 'asc' | 'desc' | null;    onDateSortToggle: () => void;    onNameSortToggle?: () => void;    onReset?: () => void;    placeholder?: string;}/** * SearchAndSort: High-fidelity component for listing pages. * Features: Integrated premium search bar with focus effects and animated sort buttons. */export const SearchAndSort: React.FC<SearchAndSortProps> = ({    searchQuery,    onSearchChange,    dateSort,    nameSort = null,    onDateSortToggle,    onNameSortToggle = () => {},    onReset = () => {},    placeholder}) => {    const { t } = useLanguage();    const showReset = searchQuery !== '' || dateSort !== 'desc' || nameSort !== null;    const activePlaceholder = placeholder || t('common:search');    return (        <div className="flex flex-col gap-6 mb-10">            {/* Premium Search Bar Container */}            <div className="relative group">                <motion.div                     initial={false}                    animate={{                         boxShadow: searchQuery ? '0 10px 25px -5px rgba(117, 10, 50, 0.1)' : '0 4px 6px -1px rgba(0, 0, 0, 0.05)',                        borderColor: searchQuery ? 'rgba(117, 10, 50, 0.3)' : 'rgba(222, 185, 65, 0.2)'                    }}                    className="relative flex items-center bg-white rounded-2xl border-2 transition-all duration-300 overflow-hidden"                >                    {/* Search Icon with focus animation */}                    <div className="pl-6 text-gold group-focus-within:text-rosewood transition-colors duration-300">                        <Search size={22} strokeWidth={2.5} />                    </div>                                        <input                        type="text"                        value={searchQuery}                        onChange={(e) => onSearchChange(e.target.value)}                        placeholder={activePlaceholder}                        className="w-full px-4 py-5 bg-transparent border-none focus:ring-0 text-rosewood font-semibold placeholder:text-gold-soft text-sm md:text-base outline-none"                    />                    {/* Animated Clear Button */}                    <AnimatePresence>                        {searchQuery && (                            <motion.button                                initial={{ opacity: 0, scale: 0.8, x: 10 }}                                animate={{ opacity: 1, scale: 1, x: 0 }}                                exit={{ opacity: 0, scale: 0.8, x: 10 }}                                whileHover={{ scale: 1.1 }}                                whileTap={{ scale: 0.9 }}                                onClick={() => onSearchChange('')}                                className="mr-4 p-2 bg-gold-soft/5 hover:bg-gold-soft/15 rounded-xl text-rosewood/40 hover:text-rosewood transition-colors"                            >                                <X size={18} strokeWidth={3} />                            </motion.button>                        )}                    </AnimatePresence>                                        {/* Dynamic Background Glow */}                    <motion.div                         layoutId="search-glow"                        className="absolute inset-0 pointer-events-none opacity-0 group-focus-within:opacity-100 transition-opacity duration-500"                        style={{                             background: 'radial-gradient(circle at center, rgba(222, 185, 65, 0.03) 0%, transparent 70%)'                         }}                    />                </motion.div>            </div>            {/* Sort Controls & Action Bar */}            <div className="flex flex-col sm:flex-row items-center justify-between gap-4 px-1">                <div className="flex items-center gap-3 self-start sm:self-center">                    <div className="p-2.5 bg-ivory rounded-xl border border-gold-soft/10 shadow-sm">                        <SlidersHorizontal size={18} className="text-rosewood/60" />                    </div>                    <div className="flex flex-col">                        <span className="text-[10px] font-black uppercase tracking-[0.2em] text-rosewood/30">                            Filtering                        </span>                        <span className="text-xs font-bold text-rosewood/60">                            {t('adminMatrimony.common.sortRecords') || 'Sort Records'}                        </span>                    </div>                </div>                <div className="flex items-center gap-3 w-full sm:w-auto">                    {/* Sort Pills Container */}                    <div className="flex items-center gap-2 p-1.5 bg-ivory/80 backdrop-blur-md rounded-2xl border border-gold-soft/20 shadow-inner-sm flex-1 sm:flex-none">                        <SortButton                             label={t('adminMatrimony.common.date') || 'Date'}                             active={dateSort !== null}                             direction={dateSort}                             onClick={onDateSortToggle}                         />                        <SortButton                             label={t('adminMatrimony.common.name') || 'Name'}                             active={nameSort !== null}                             direction={nameSort}                             onClick={onNameSortToggle}                         />                    </div>                    {/* Animated Reset Button */}                    <AnimatePresence>                        {showReset && (                            <motion.button                                initial={{ opacity: 0, scale: 0.5, rotate: -180 }}                                animate={{ opacity: 1, scale: 1, rotate: 0 }}                                exit={{ opacity: 0, scale: 0.5, rotate: -180 }}                                whileHover={{ scale: 1.1, rotate: -45 }}                                whileTap={{ scale: 0.9 }}                                onClick={onReset}                                className="w-12 h-12 flex items-center justify-center bg-white text-rosewood/40 hover:text-rosewood rounded-2xl transition-all border border-gold-soft/20 shadow-sm group relative overflow-hidden"                                title={t('adminMatrimony.common.reset') || 'Reset'}                            >                                <RotateCcw size={20} />                                <motion.div                                     className="absolute inset-0 bg-gold-soft/5 opacity-0 group-hover:opacity-100 transition-opacity"                                />                            </motion.button>                        )}                    </AnimatePresence>                </div>            </div>        </div>    );};const SortButton: React.FC<{     label: string;     active: boolean;     direction: 'asc' | 'desc' | null;     onClick: () => void }> = ({ label, active, direction, onClick }) => (    <button        onClick={onClick}        className={`            flex-1 sm:flex-none flex items-center justify-center gap-2.5 px-6 py-3 rounded-xl text-[10px] md:text-xs font-black uppercase tracking-widest transition-all duration-300 relative            ${active                 ? 'bg-rosewood text-white shadow-lg shadow-rosewood/20 scale-[1.02]'                 : 'text-rosewood/60 hover:text-rosewood hover:bg-white'            }        `}    >        {label}        <div className="flex items-center min-w-[14px]">            {direction === 'asc' ? (                <ArrowUp size={14} strokeWidth={3} className="animate-in slide-in-from-bottom-1 duration-300" />            ) : direction === 'desc' ? (                <ArrowDown size={14} strokeWidth={3} className="animate-in slide-in-from-top-1 duration-300" />            ) : (                <ArrowUpDown size={14} className="opacity-20" />            )}        </div>    </button>);