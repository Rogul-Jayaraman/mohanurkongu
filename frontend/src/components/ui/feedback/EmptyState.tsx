import React from 'react';import { LucideIcon } from 'lucide-react';interface EmptyStateProps {    message: string;    description?: string;    icon?: LucideIcon | React.ReactNode;    className?: string;    variant?: 'dashed' | 'simple';}/** * Consistent Empty State pattern for lists and grids */export const EmptyState: React.FC<EmptyStateProps> = ({     message,     description,    icon: Icon,     className = "",    variant = 'dashed'}) => {    return (        <div className={`py-20 px-6 text-center rounded-xl flex flex-col items-center justify-center ${            variant === 'dashed' ? 'bg-ivory-tint/30 border-2 border-gold/10 border-dashed' : 'bg-transparent'        } ${className}`}>            {Icon && (                <div className="mb-4 text-gold/20">                    {typeof Icon === 'function' ? (                        <Icon size={48} strokeWidth={1} />                    ) : (Icon && typeof Icon === 'object' && 'render' in Icon) ? (                        // @ts-ignore                        <Icon size={48} strokeWidth={1} />                    ) : (                        Icon                    )}                </div>            )}            <p className="text-rosewood font-serif italic text-lg opacity-60">                {message}            </p>            {description && (                <p className="text-xs text-slate-400 mt-2 max-w-xs mx-auto leading-relaxed">                    {description}                </p>            )}        </div>    );};