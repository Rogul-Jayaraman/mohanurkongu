import React, { useEffect, useState } from 'react';import { useLazyImage } from '@/hooks/useLazyImage';interface LazyBackgroundProps extends React.HTMLAttributes<HTMLDivElement> {  src: string;  placeholder?: string;  priority?: boolean;  alt?: string;  containerClassName?: string;}export const LazyBackground: React.FC<LazyBackgroundProps> = ({  src,  placeholder = 'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==',  priority,  alt,  className = '',  containerClassName = '',  style = {},  children,  ...props}) => {  // Load the high‑res image lazily (not priority)  const { ref, source, isLoaded } = useLazyImage({ src, priority: priority ?? false });  const [bgUrl, setBgUrl] = useState<string>(placeholder ?? '');  // When the high‑res image finishes loading, update background URL  useEffect(() => {    if (isLoaded && source) {      setBgUrl(source);    }  }, [isLoaded, source]);  const combinedStyle: React.CSSProperties = {    ...style,    backgroundImage: `url(${bgUrl})`,    backgroundSize: 'cover',    backgroundPosition: 'center',    backgroundRepeat: 'no-repeat',    transition: isLoaded ? 'background-image 0.5s ease-in-out' : undefined,  };  return (    <div       ref={ref}       className={`${containerClassName} ${className}`.trim()}       style={combinedStyle}      role="img"      aria-label={alt}      {...props}    >      {children}    </div>  );};