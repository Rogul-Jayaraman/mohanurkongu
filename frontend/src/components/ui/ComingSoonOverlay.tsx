import React from 'react';import { motion } from 'framer-motion';import { useTranslation } from 'react-i18next';import { useLanguage } from '@/context/LanguageContext';export const ComingSoonOverlay: React.FC = () => {  const { language: lang } = useLanguage();  const { t } = useTranslation('landing');  return (    <motion.div      initial={{ opacity: 0 }}      animate={{ opacity: 1 }}      className="absolute inset-0 z-50 flex items-center justify-center p-6"    >      <div className="absolute inset-0 backdrop-blur-xs bg-ivory/30" />      <motion.div        initial={{ opacity: 0, scale: 0.9, y: 10 }}        animate={{ opacity: 1, scale: 1, y: 0 }}         transition={{ delay: 0.1, duration: 0.5 }}        className="relative z-10 flex flex-col items-center"      >        <div className="mb-4">          <span className="material-symbols-outlined text-gold-500 text-sm leading-none">            auto_awesome          </span>        </div>        <h3 className={`          ${lang === 'ta' ? 'text-lg md:text-xl' : 'text-xl md:text-2xl'}           font-heading font-bold text-rosewood tracking-tight text-center        `}>          {t('common.comingSoon')}        </h3>        <div className="w-12 h-0.5 bg-gold-500/30 mt-3 rounded-full overflow-hidden">          <motion.div            animate={{ x: [-48, 48] }}            transition={{ duration: 2, repeat: Infinity }}            className="w-full h-full bg-linear-to-r from-transparent via-gold-500 to-transparent"          />        </div>      </motion.div>      <div className="absolute inset-0 pointer-events-none border border-white/40 rounded-[inherit]" />    </motion.div>  );};export default ComingSoonOverlay;