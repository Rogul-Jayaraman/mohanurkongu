import React from 'react';import { useDualScript } from '@/hooks/useDualScript';import { TamilKeyboard } from './TamilKeyboard';interface DualScriptFieldProps {    value: string;    onChange: (value: string) => void;    placeholder?: string;    label?: string;    className?: string;    disabled?: boolean;}export const DualScriptField: React.FC<DualScriptFieldProps> = ({    value,    onChange,    placeholder,    label,    className = '',    disabled = false,}) => {    const inputRef = React.useRef<HTMLInputElement>(null);    const {        preview,        isPending,        isKeyboardOpen,        openKeyboard,        closeKeyboard,        toggleKeyboard,        insertKey,        handleBackspace,    } = useDualScript({ text: value, targetLanguage: 'ta' });    const handleKeyboardSelect = (char: string) => {        const input = inputRef.current;        if (!input) return;        const newState = insertKey({            text: value,            cursorPosition: input.selectionStart || 0,        }, char);        onChange(newState.text);        setTimeout(() => {            input.focus();            input.setSelectionRange(newState.cursorPosition, newState.cursorPosition);        }, 0);    };    const handleKeyboardBackspace = () => {        const input = inputRef.current;        if (!input) return;        const newState = handleBackspace({            text: value,            cursorPosition: input.selectionStart || 0,        });        onChange(newState.text);        setTimeout(() => {            input.focus();            input.setSelectionRange(newState.cursorPosition, newState.cursorPosition);        }, 0);    };    return (        <div className={`w-full space-y-2 ${className}`}>            {label && (                <label className="block text-[11px] sm:text-xs font-bold text-rosewood tracking-tight">                    {label}                </label>            )}            <div className="relative group">                <input                    ref={inputRef}                    type="text"                    value={value}                    onChange={(e) => onChange(e.target.value)}                    placeholder={placeholder}                    disabled={disabled}                    className="w-full h-12 px-4 pr-12 rounded-xl border border-input-border bg-input-bg text-sm text-input-text placeholder:text-input-placeholder outline-none focus:border-input-focus focus:ring-4 focus:ring-input-ring transition-all disabled:opacity-70 disabled:cursor-not-allowed"                />                {isPending && (                    <div className="absolute right-12 top-1/2 -translate-y-1/2">                        <div className="w-1.5 h-1.5 rounded-full bg-gold animate-ping opacity-75" />                    </div>                )}                <button                    type="button"                    onClick={toggleKeyboard}                    className={`absolute right-3 top-1/2 -translate-y-1/2 size-8 flex items-center justify-center rounded-lg transition-all ${                        isKeyboardOpen                            ? 'bg-rosewood text-white shadow-lg'                            : 'bg-gold/5 text-rosewood hover:bg-gold/10 border border-gold/10'                    }`}                    title="Tamil Keyboard"                >                    <span className="material-symbols-outlined text-[18px]">keyboard</span>                </button>            </div>            <TamilKeyboard                isOpen={isKeyboardOpen}                onClose={closeKeyboard}                onKeyPress={handleKeyboardSelect}                onBackspace={handleKeyboardBackspace}                onSpace={() => handleKeyboardSelect(' ')}                onEnter={closeKeyboard}            />        </div>    );};