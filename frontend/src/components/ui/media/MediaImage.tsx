import React from 'react';interface ImageObject {  url: string;  width?: number | null;  height?: number | null;}interface MediaImageProps {  image?: ImageObject | null;  uploadId?: string | null;  alt?: string;  className?: string;  fallback?: React.ReactNode;  onClick?: React.MouseEventHandler<HTMLImageElement>;  priority?: 'auto' | 'high' | 'low';}const getMediaSrc = (urlOrId: string): string => {  if (/^(https?:\/\/)/.test(urlOrId)) return urlOrId;  const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:4000';  if (urlOrId.startsWith('/media/')) return `${API_BASE}${urlOrId}`;  if (/^upl_[0-9a-f]{12}$/i.test(urlOrId)) {    return `${API_BASE}/media/by-token/${urlOrId}`;  }  return `${API_BASE}/media/${urlOrId}.webp`;};export const MediaImage: React.FC<MediaImageProps> = ({  image,  uploadId,  alt = '',  className = '',  fallback,  onClick,  priority,}) => {  const src = image?.url ? getMediaSrc(image.url) : (uploadId ? getMediaSrc(uploadId) : null);  if (!src) {    return fallback ? <>{fallback}</> : null;  }  return (    <img      src={src}      alt={alt}      width={image?.width ?? undefined}      height={image?.height ?? undefined}      className={className}      loading="lazy"      decoding="async"      fetchPriority={priority}      onClick={onClick}    />  );};