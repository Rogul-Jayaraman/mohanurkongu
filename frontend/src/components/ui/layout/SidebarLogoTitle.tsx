import React from 'react';import { motion, AnimatePresence } from 'framer-motion';import { Link } from 'react-router-dom';import { useLanguage } from '@/context/LanguageContext';interface SidebarLogoTitleProps {  brandKey?: string;  taglineKey?: string;  showClose?: boolean;  onClose?: () => void;}const SidebarLogoTitle: React.FC<SidebarLogoTitleProps> = ({  brandKey = 'brand',  taglineKey = 'tagline',  showClose,  onClose,}) => {  const { language, t } = useLanguage();  return (    <div className="p-6 md:p-8 flex items-center justify-between gap-3">      <Link        to="/"        className="flex items-center gap-3 group py-2"      >        <motion.img          src="/mohanur_kongu/images/brand/logo.webp"          alt="Mohanur Kongu Logo"          className="h-10 md:h-14 w-auto rounded-full border-2 border-gold/20 shadow-sm md:shadow-md group-hover:scale-110 transition-transform duration-500"          initial={{ opacity: 0, scale: 0.8 }}          animate={{ opacity: 1, scale: 1 }}        />        <div className="flex flex-row items-baseline gap-2">          <AnimatePresence mode="wait" initial={false}>            <motion.div              key={language}              initial={{ opacity: 0, x: 8 }}              animate={{ opacity: 1, x: 0 }}              exit={{ opacity: 0, x: -8 }}              transition={{ duration: 0.2, ease: 'easeOut' }}              className="flex flex-col gap-1"            >              <h1                className="font-serif font-bold text-rosewood leading-none group-hover:text-dark-rosewood/80 transition-all whitespace-nowrap text-sm lg:text-lg"              >                {t(brandKey)}              </h1>              <p                className="font-serif font-bold text-rosewood leading-none group-hover:text-dark-rosewood/80 transition-all whitespace-nowrap text-sm lg:text-lg"              >                {t(taglineKey)}              </p>            </motion.div>          </AnimatePresence>        </div>      </Link>      {showClose && (        <button          className="xl:hidden text-gray-500 p-1 hover:text-rosewood hover:rotate-90 transition-all duration-300"          onClick={onClose}        >          <span className="material-symbols-outlined">close</span>        </button>      )}    </div>  );};export default SidebarLogoTitle;