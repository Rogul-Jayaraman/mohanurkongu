import { useEffect } from 'react';import { useLocation } from 'react-router-dom';/** * Function to scroll the main content area to the top * Used by both the ScrollToTop component and the useScrollToTop hook */export const scrollToTop = () => {    // Use double requestAnimationFrame to ensure DOM is fully ready    requestAnimationFrame(() => {        requestAnimationFrame(() => {            // Scroll all potential containers with the specific class            const scrollContainers = document.querySelectorAll('.main-content-scroll');            if (scrollContainers.length > 0) {                scrollContainers.forEach(container => {                    container.scrollTop = 0;                });            }                        // Always scroll window and document elements to top as fallback            window.scrollTo(0, 0);            document.documentElement.scrollTop = 0;            if (document.body) {                document.body.scrollTop = 0;            }        });    });};/** * Hook to manually trigger scroll to top * Useful for wizard step changes, modal open/close, etc. */export const useScrollToTop = (dependencies: any[] = []) => {    useEffect(() => {        scrollToTop();    }, dependencies);};/** * ScrollToTop Component * Automatically scrolls the main content area to the top when: * - Route changes * - Can be extended with additional dependencies for wizard steps, modals, etc. */export const ScrollToTop: React.FC<{ dependencies?: any[] }> = ({ dependencies = [] }) => {    const { pathname } = useLocation();    useEffect(() => {        scrollToTop();    }, [pathname, ...dependencies]);    // Disable browser's automatic scroll restoration    useEffect(() => {        if ('scrollRestoration' in window.history) {            window.history.scrollRestoration = 'manual';        }                // Scroll to top on initial load        scrollToTop();    }, []);    return null;};