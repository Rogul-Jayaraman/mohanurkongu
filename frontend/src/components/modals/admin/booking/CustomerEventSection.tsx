import React, { useState, useMemo } from 'react';import type { UseFormReturn } from 'react-hook-form';import type { BookingFormData } from './bookingFormTypes';import { Input } from '@/components/ui/forms/Input';import { PhoneInput } from '@/components/ui/forms/PhoneInput';import { EmailField } from '@/components/ui/forms/EmailField';import Select from '@/components/ui/forms/Select';import { TransliteratedPreview } from '@/components/ui/forms/TransliteratedPreview';import { useLanguage } from '@/context/LanguageContext';import { DISTRICTS, TALUKS_BY_DISTRICT, DISTRICT_TAMIL, TALUK_TAMIL } from '@/constants/locations';interface CustomerEventSectionProps {  form: UseFormReturn<BookingFormData>;}const districtToStringOptions = (arr: string[]) =>  arr.map(s => ({    value: s,    label: {      en: s.toLowerCase().split(' ').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' '),      ta: DISTRICT_TAMIL[s] || s,    },  }));const talukToStringOptions = (arr: string[]) =>  arr.map(s => ({    value: s,    label: {      en: s.toLowerCase().split(' ').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' '),      ta: TALUK_TAMIL[s] || s,    },  }));export const CustomerEventSection: React.FC<CustomerEventSectionProps> = ({ form }) => {  const { language } = useLanguage();  const isTamil = language === 'ta';  const [isNameFocused, setIsNameFocused] = useState(false);  const [isEventFocused, setIsEventFocused] = useState(false);  const [isLandmarkFocused, setIsLandmarkFocused] = useState(false);  const errors = form.formState.errors;  const currentName = isTamil ? form.watch('contactName.ta') : form.watch('contactName.en');  const currentAlternateName = isTamil ? form.watch('contactName.en') : form.watch('contactName.ta');  const nameField = isTamil ? 'contactName.ta' : 'contactName.en';  const alternateNameField = isTamil ? 'contactName.en' : 'contactName.ta';  const currentEvent = isTamil ? form.watch('eventName.ta') : form.watch('eventName.en');  const currentAlternateEvent = isTamil ? form.watch('eventName.en') : form.watch('eventName.ta');  const eventField = isTamil ? 'eventName.ta' : 'eventName.en';  const alternateEventField = isTamil ? 'eventName.en' : 'eventName.ta';  const currentLandmark = isTamil ? form.watch('landmark.ta') : form.watch('landmark.en');  const currentAlternateLandmark = isTamil ? form.watch('landmark.en') : form.watch('landmark.ta');  const landmarkField = isTamil ? 'landmark.ta' : 'landmark.en';  const alternateLandmarkField = isTamil ? 'landmark.en' : 'landmark.ta';  const selectedDistrict = form.watch('district');  const talukOptions = useMemo(() => {    if (!selectedDistrict || selectedDistrict === 'OTHER' || !TALUKS_BY_DISTRICT[selectedDistrict]) return [];    return talukToStringOptions(TALUKS_BY_DISTRICT[selectedDistrict]);  }, [selectedDistrict]);  return (    <div className="space-y-4">      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">        <div className="space-y-1">          <Input            label={isTamil ? 'பெயர்' : 'Name'}            icon="person"            name={nameField}            value={currentName}            onChange={(e) => {              let val = e.target.value;              if (!isTamil && val.length > 0) {                val = val.charAt(0).toUpperCase() + val.slice(1);              }              form.setValue(nameField as any, val);            }}            targetLanguage={isTamil ? 'ta' : undefined}            onFocus={() => setIsNameFocused(true)}            onBlur={() => setIsNameFocused(false)}            placeholder={isTamil ? 'வாடிக்கையாளர் பெயர்' : 'e.g. Rajesh Kumar'}            error={errors.contactName?.en?.message}            required          />          <TransliteratedPreview            key={isTamil ? 'en' : 'ta'}            text={currentName}            value={currentAlternateName}            mode="title"            isFocused={isNameFocused}            onPreviewChange={(confirmed) => {              form.setValue(alternateNameField as any, confirmed);            }}          />        </div>        <div className="space-y-1">          <Input            label={isTamil ? 'நிகழ்வு பெயர்' : 'Event Name'}            icon="celebration"            name={eventField}            value={currentEvent}            onChange={(e) => {              let val = e.target.value;              if (!isTamil && val.length > 0) {                val = val.charAt(0).toUpperCase() + val.slice(1);              }              form.setValue(eventField as any, val);            }}            targetLanguage={isTamil ? 'ta' : undefined}            onFocus={() => setIsEventFocused(true)}            onBlur={() => setIsEventFocused(false)}            placeholder={isTamil ? 'நிகழ்வின் பெயர்' : 'e.g. Wedding Ceremony'}            error={errors.eventName?.en?.message}            required          />          <TransliteratedPreview            key={isTamil ? 'en' : 'ta'}            text={currentEvent}            value={currentAlternateEvent}            mode="title"            isFocused={isEventFocused}            onPreviewChange={(confirmed) => {              form.setValue(alternateEventField as any, confirmed);            }}          />        </div>      </div>      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">        <PhoneInput          label={isTamil ? 'தொலைபேசி எண்' : 'Phone Number'}          name="phone"          defaultValue={form.watch('phone')}          onChange={(val) => form.setValue('phone', val)}          required        />        <EmailField          label={isTamil ? 'மின்னஞ்சல்' : 'Email Address'}          name="email"          value={form.watch('email')}          onChange={(e) => form.setValue('email', e.target.value)}          placeholder="rajesh@example.com"        />        {errors.phone?.message && (          <p className="text-[10px] font-bold text-red-500 md:col-span-2 -mt-3">{errors.phone.message}</p>        )}      </div>      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">        <Input          label={isTamil ? 'கதவு எண் / முகவரி' : 'Door No / Address'}          icon="location_on"          name="doorNo"          value={form.watch('doorNo') || ''}          onChange={(e) => form.setValue('doorNo', e.target.value)}          placeholder={isTamil ? 'எ.கா: 2/3-ஏ, மெயின் ரோடு' : 'e.g. 2/3-A, Main Road'}        />        <div className="space-y-1">          <Input            label={isTamil ? 'அஞ்சல் / அடையாளம்' : 'Post / Landmark'}            icon="signpost"            name={landmarkField}            value={currentLandmark || ''}            onChange={(e) => form.setValue(landmarkField as any, e.target.value)}            targetLanguage={isTamil ? 'ta' : undefined}            onFocus={() => setIsLandmarkFocused(true)}            onBlur={() => setIsLandmarkFocused(false)}            placeholder={isTamil ? 'அஞ்சல் நிலையம் அல்லது அடையாளம்' : 'e.g. Near Bus Stand'}          />          <TransliteratedPreview            key={isTamil ? 'en' : 'ta'}            text={currentLandmark || ''}            value={currentAlternateLandmark || ''}            mode="title"            isFocused={isLandmarkFocused}            onPreviewChange={(confirmed) => {              form.setValue(alternateLandmarkField as any, confirmed);            }}          />        </div>      </div>      <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">        <Select          label={isTamil ? 'மாவட்டம்' : 'District'}          value={selectedDistrict || ''}          onChange={(val) => {            form.setValue('district', val);            form.setValue('taluk', '');          }}          options={districtToStringOptions(DISTRICTS)}          placeholder={isTamil ? 'மாவட்டத்தைத் தேர்ந்தெடுக்கவும்' : 'Select District'}        />        <Select          label={isTamil ? 'தாலுகா' : 'Taluk'}          value={form.watch('taluk') || ''}          onChange={(val) => form.setValue('taluk', val)}          options={talukOptions}          disabled={!selectedDistrict || selectedDistrict === 'OTHER'}          placeholder={!selectedDistrict            ? (isTamil ? 'முதலில் மாவட்டத்தைத் தேர்ந்தெடுக்கவும்' : 'Select District First')            : (isTamil ? 'தாலுகாவைத் தேர்ந்தெடுக்கவும்' : 'Select Taluk')          }        />      </div>    </div>  );};