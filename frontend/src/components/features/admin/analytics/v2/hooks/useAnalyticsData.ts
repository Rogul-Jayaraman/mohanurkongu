import { useState, useEffect, useCallback, useRef } from 'react';import { fetchManamaalaiAnalytics, fetchMandapamAnalytics } from '@/api/analytics.api';import type { ManamaalaiAnalytics, MandapamAnalytics } from '@/types/analytics';export type SectionKey = 'manamaalai' | 'mandapam';interface SectionState<T> {  data: T | null;  loading: boolean;  error: string | null;  cachedAt: number | null;}type AnalyticsState = {  [K in SectionKey]: SectionState<    K extends 'manamaalai' ? ManamaalaiAnalytics : MandapamAnalytics  >;};interface UseAnalyticsDataResult {  sections: AnalyticsState;  loading: boolean;  error: string | null;  refetch: () => void;  refetchSection: (section: SectionKey) => void;  manamaalai: ManamaalaiAnalytics | null;  mandapam: MandapamAnalytics | null;  activeSection: SectionKey;  setActiveSection: (s: SectionKey) => void;}const FETCH_TIMEOUT_MS = 25_000;const SECTION_FETCHERS: Record<SectionKey, () => Promise<any>> = {  manamaalai: fetchManamaalaiAnalytics,  mandapam: fetchMandapamAnalytics,};const INITIAL_SECTION: SectionState<any> = { data: null, loading: false, error: null, cachedAt: null };function initialAnalyticsState(): AnalyticsState {  return {    manamaalai: { ...INITIAL_SECTION, loading: true },    mandapam: { ...INITIAL_SECTION },  };}function fetchWithTimeout<T>(fetcher: () => Promise<T>, timeoutMs: number): Promise<T> {  const ac = new AbortController();  const timer = setTimeout(() => ac.abort(), timeoutMs);  return fetcher().finally(() => clearTimeout(timer));}export function useAnalyticsData(): UseAnalyticsDataResult {  const [activeSection, setActiveSection] = useState<SectionKey>('manamaalai');  const [sections, setSections] = useState<AnalyticsState>(initialAnalyticsState);  const fetchIdRef = useRef(0);  const safeSet = useCallback((key: SectionKey, updater: (prev: SectionState<any>) => SectionState<any>) => {    setSections(prev => ({ ...prev, [key]: updater(prev[key]) }));  }, []);  const fetchOne = useCallback((key: SectionKey) => {    const id = ++fetchIdRef.current;    safeSet(key, s => ({ ...s, loading: true, error: null }));    fetchWithTimeout(SECTION_FETCHERS[key], FETCH_TIMEOUT_MS)      .then((data: any) => {        if (id === fetchIdRef.current) {          safeSet(key, () => ({ data, loading: false, error: null, cachedAt: Date.now() }));        }      })      .catch((err: any) => {        if (id === fetchIdRef.current) {          const message = err?.message ?? `Failed to load ${key}`;          console.error(`[Analytics] ${key} fetch failed:`, err);          safeSet(key, s => ({ ...s, loading: false, error: message }));        }      });  }, [safeSet]);  useEffect(() => { fetchOne(activeSection); }, [activeSection, fetchOne]);  const refetch = useCallback(() => { fetchOne(activeSection); }, [activeSection, fetchOne]);  const refetchSection = useCallback((key: SectionKey) => { fetchOne(key); }, [fetchOne]);  const activeState = sections[activeSection];  return {    sections,    loading: activeState.loading,    error: activeState.error,    refetch,    refetchSection,    manamaalai: sections.manamaalai.data,    mandapam: sections.mandapam.data,    activeSection,    setActiveSection,  };}