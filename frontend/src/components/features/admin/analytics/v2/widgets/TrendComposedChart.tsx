import React from 'react';import { ComposedChart, Line, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend } from 'recharts';import { EmptyState } from '@/components/ui/feedback/EmptyState';import { TrendingUp } from 'lucide-react';interface Series {  key: string; type: 'bar' | 'line'; color: string; name: string; yAxisId?: string;}interface Props {  title: string; subtitle?: string; data: Record<string, any>[]; series: Series[];  xKey: string; loading?: boolean; height?: number;}export const TrendComposedChart: React.FC<Props> = React.memo(({ title, subtitle, data, series, xKey, loading, height = 300 }) => {  if (loading) {    return (      <div className="bg-white border border-gold/20 rounded-xl shadow-sm animate-pulse">        <div className="p-5 border-b border-gold/10"><div className="h-4 w-44 bg-rosewood/10 rounded" />{subtitle && <div className="h-3 w-28 bg-rosewood/5 rounded mt-1.5" />}</div>        <div className="h-[260px] bg-ivory-tint/30 m-4 rounded-lg" />      </div>    );  }  return (    <div className="bg-white border border-gold/20 rounded-xl shadow-sm">      <div className="px-6 py-4 border-b border-gold/10">        <h3 className="font-serif font-bold text-rosewood">{title}</h3>        {subtitle && <p className="text-xs text-dark-brown/60 mt-0.5">{subtitle}</p>}      </div>      {data.length === 0 ? (        <div className="p-6"><EmptyState message="No trend data yet" icon={TrendingUp} variant="dashed" /></div>      ) : (        <div className="p-4"><div style={{ height }}>          <ResponsiveContainer width="100%" height="100%">            <ComposedChart data={data} margin={{ top: 8, right: 8, left: -8, bottom: 0 }}>              <CartesianGrid strokeDasharray="3 3" stroke="#F3F4F6" vertical={false} />              <XAxis dataKey={xKey} axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: '#9CA3AF' }} dy={6} />              <YAxis yAxisId="left" axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: '#9CA3AF' }} />              <YAxis yAxisId="right" orientation="right" axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: '#9CA3AF' }} />              <Tooltip content={({ active, payload, label }: any) => {                if (!active || !payload?.length) return null;                return <div className="bg-white/95 border border-gold/20 rounded-xl shadow-lg px-4 py-3 text-xs"><p className="font-serif font-bold text-rosewood mb-1">{label}</p>{payload.map((p: any, i: number) => <p key={i} className="text-slate-600" style={{ color: p.color }}>{p.name}: <span className="font-bold">{typeof p.value === 'number' ? p.value.toLocaleString() : p.value}</span></p>)}</div>;              }} />              <Legend iconType="circle" iconSize={8} wrapperStyle={{ fontSize: 11, paddingTop: 8 }} />              {series.map(s => {                if (s.type === 'bar') return <Bar key={s.key} yAxisId={s.yAxisId || 'left'} dataKey={s.key} fill={s.color} name={s.name} radius={[4, 4, 0, 0]} barSize={12} />;                return <Line key={s.key} yAxisId={s.yAxisId || 'left'} type="monotone" dataKey={s.key} stroke={s.color} name={s.name} strokeWidth={2} dot={{ r: 3, fill: s.color }} activeDot={{ r: 5 }} />;              })}            </ComposedChart>          </ResponsiveContainer>        </div></div>      )}    </div>  );});TrendComposedChart.displayName = 'TrendComposedChart';