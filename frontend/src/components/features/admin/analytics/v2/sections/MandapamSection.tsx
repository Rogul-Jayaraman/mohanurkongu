import React from 'react';import { motion } from 'framer-motion';import { useTranslation } from 'react-i18next';import type { MandapamAnalytics } from '@/types/analytics';import { MetricsGrid } from '../widgets/MetricsGrid';import { TrendComposedChart } from '../widgets/TrendComposedChart';import { DonutCard } from '../widgets/DonutCard';import { StackedBarCard } from '../widgets/StackedBarCard';import { Calendar, DollarSign, BarChart3, TrendingUp, ClipboardList } from 'lucide-react';import { CHART } from '@/constants/analyticsColors';interface Props { data: MandapamAnalytics | null; loading: boolean }const container = { hidden: {}, show: { transition: { staggerChildren: 0.06 } } };const fadeUp = { hidden: { opacity: 0, y: 16 }, show: { opacity: 1, y: 0 } };const CardSection: React.FC<{ title: string; subtitle?: string; icon?: React.ElementType; children: React.ReactNode; className?: string }> = ({ title, subtitle, icon: Icon, children, className = '' }) => (  <div className={`relative bg-white/10 backdrop-blur-2xl border-2 border-gold/30 rounded-xl overflow-hidden ${className}`}>    <div className="absolute inset-0 bg-linear-to-br from-white/40 to-white/5 rounded-xl pointer-events-none" />    <div className="absolute top-0 right-0 w-40 h-40 bg-gold/20 rounded-full -translate-y-1/2 translate-x-1/2 blur-3xl pointer-events-none" />    <div className="absolute bottom-0 left-0 w-32 h-32 bg-gold/20 rounded-full translate-y-1/2 -translate-x-1/2 blur-3xl pointer-events-none" />    <div className="relative z-10 flex flex-col">      <div className="px-6 py-5 bg-linear-to-r from-ivory/80 via-gold-soft/10 to-ivory/80 backdrop-blur-xl border-b border-gold/10">        <div className="flex items-center gap-3">          {Icon && (            <div className="size-9 rounded-xl bg-rosewood-gradient flex items-center justify-center shadow-md shrink-0">              <Icon size={16} className="text-gold" />            </div>          )}          <div>            <h3 className="text-lg font-serif font-bold text-rosewood">{title}</h3>            {subtitle && <p className="text-xs text-dark-brown/60 mt-0.5">{subtitle}</p>}          </div>        </div>      </div>      <div className="p-6">{children}</div>    </div>  </div>);export const MandapamSection: React.FC<Props> = ({ data, loading }) => {  const { t } = useTranslation('analytics');  const o = data?.overview;  const overviewMetrics = o ? [    { label: t('metrics.occupancyRate'), value: `${Math.round(o.occupancyRate)}%` },    { label: t('metrics.activeBookings'), value: o.activeBookings },    { label: t('metrics.monthRevenue'), value: `₹${(o.revenueMTD / 100000).toFixed(1)}L` },    { label: t('metrics.dueAmount'), value: `₹${(o.outstandingBalance / 100000).toFixed(1)}L` },  ] : [];  const weeklyMetrics = o ? [    { label: t('metrics.revenueChange'), value: `${o.revenueWoW >= 0 ? '+' : ''}${o.revenueWoW}%` },    { label: t('metrics.occupancyChange'), value: `${o.occupancyWoW >= 0 ? '+' : ''}${o.occupancyWoW}%` },    { label: t('metrics.bookingChange'), value: `${o.bookingWoW >= 0 ? '+' : ''}${o.bookingWoW}%` },    { label: t('metrics.dueAmountChange'), value: `${o.outstandingWoW >= 0 ? '+' : ''}${o.outstandingWoW}%` },  ] : [];  const lifecycle = data?.bookingLifecycle ? [    { name: t('charts.lifecycle.confirmed'), value: data.bookingLifecycle.confirmed, color: CHART.lifecycle.confirmed },    { name: t('charts.lifecycle.eventInProgress'), value: data.bookingLifecycle.eventInProgress, color: CHART.lifecycle.eventInProgress },    { name: t('charts.lifecycle.settlementPending'), value: data.bookingLifecycle.settlementPending, color: CHART.lifecycle.settlementPending },    { name: t('charts.lifecycle.completed'), value: data.bookingLifecycle.completed, color: CHART.lifecycle.completed },    { name: t('charts.lifecycle.cancelled'), value: data.bookingLifecycle.cancelled, color: CHART.lifecycle.cancelled },  ].filter(d => d.value > 0) : [];  const bookingStats = data?.bookingLifecycle ? [    { label: t('metrics.totalBookings'), value: Object.values(data.bookingLifecycle).reduce((a, b) => a + b, 0) },    { label: t('metrics.confirmed'), value: data.bookingLifecycle.confirmed },    { label: t('metrics.inProgress'), value: data.bookingLifecycle.eventInProgress },    { label: t('metrics.pendingSettlement'), value: data.bookingLifecycle.settlementPending },    { label: t('metrics.completed'), value: data.bookingLifecycle.completed },    { label: t('metrics.cancelled'), value: data.bookingLifecycle.cancelled },  ] : [];  const revenueKeys = [    { key: 'standard', color: CHART.revenuePackage.standard, name: t('charts.revenuePackage.standard') },    { key: 'royal', color: CHART.revenuePackage.royal, name: t('charts.revenuePackage.royal') },    { key: 'grand', color: CHART.revenuePackage.grand, name: t('charts.revenuePackage.grand') },    { key: 'addon', color: CHART.revenuePackage.addon, name: t('charts.revenuePackage.addon') },  ];  return (    <motion.div variants={container} initial="hidden" animate="show" className="space-y-6">      {/* Section 1: At a Glance */}      <motion.div variants={fadeUp}>        <div className="flex items-center gap-2 mb-3">          <h2 className="text-sm font-bold text-dark-brown">{t('sections.atAGlance')}</h2>          <div className="h-px flex-1 bg-gold/10" />        </div>        <MetricsGrid metrics={overviewMetrics} loading={loading} />      </motion.div>      {/* Section 2: Weekly Changes */}      <motion.div variants={fadeUp}>        <CardSection title={t('sections.weeklyChanges')} subtitle="Compared to the previous week" icon={TrendingUp}>          <MetricsGrid metrics={weeklyMetrics} loading={loading} />        </CardSection>      </motion.div>      {/* Section 3: Booking Trends (full width) */}      <motion.div variants={fadeUp}>        <CardSection title={t('charts.monthlyBookings')} subtitle={t('charts.monthlyBookingsSub')} icon={Calendar}>          <TrendComposedChart            title=""            data={data?.bookingTrend?.map(d => ({ ...d })) ?? []}            series={[              { key: 'bookings', type: 'bar', color: CHART.trendBar, name: t('charts.lifecycle.confirmed') },              { key: 'revenue', type: 'line', color: CHART.trendLine, name: t('metrics.monthRevenue') },            ]}            xKey="month"            loading={loading}          />        </CardSection>      </motion.div>      {/* Section 4: Booking Status & Revenue (2-column) */}      <motion.div variants={fadeUp}>        <div className="flex items-center gap-2 mb-3">          <h2 className="text-sm font-bold text-dark-brown">{t('sections.bookingStatusRevenue')}</h2>          <div className="h-px flex-1 bg-gold/10" />        </div>        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">          <CardSection title={t('charts.bookingStatus')} subtitle={t('charts.bookingStatusSub')} icon={BarChart3}>            <DonutCard              title=""              subtitle=""              data={lifecycle}              centerLabel={String(data?.bookingLifecycle ? Object.values(data.bookingLifecycle).reduce((a, b) => a + b, 0) : 0)}              loading={loading}            />          </CardSection>          <CardSection title={t('charts.revenueByPackage')} subtitle={t('charts.revenueByPackageSub')} icon={DollarSign}>            <StackedBarCard              title=""              data={data?.revenueBreakdown ?? []}              keys={revenueKeys}              xKey="month"              loading={loading}            />          </CardSection>        </div>      </motion.div>      {/* Section 5: Booking Stats */}      <motion.div variants={fadeUp}>        <div className="flex items-center gap-2 mb-3">          <h2 className="text-sm font-bold text-dark-brown">{t('sections.bookingStats')}</h2>          <div className="h-px flex-1 bg-gold/10" />        </div>        <MetricsGrid metrics={bookingStats} loading={loading} />      </motion.div>    </motion.div>  );};