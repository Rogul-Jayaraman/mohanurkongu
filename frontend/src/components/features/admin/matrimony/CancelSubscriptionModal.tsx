import React, { useState } from 'react';import { motion, AnimatePresence } from 'framer-motion';import { Crown, RotateCcw, XCircle, Loader2, AlertTriangle } from 'lucide-react';import { ModalShell } from '@/components/ui/modals/ModalShell';import { useLanguage } from '@/context/LanguageContext';interface CancelSubscriptionModalProps {  isOpen: boolean;  onClose: () => void;  onConfirm: (action: 'cancel' | 'revert') => Promise<void>;  currentPlanName: string;  revertablePlanName?: string | null;}const CancelSubscriptionModal: React.FC<CancelSubscriptionModalProps> = ({  isOpen, onClose, onConfirm, currentPlanName, revertablePlanName,}) => {  const { t, language } = useLanguage();  const isTamil = language === 'ta';  const [selectedAction, setSelectedAction] = useState<'revert' | 'cancel' | null>(null);  const [confirming, setConfirming] = useState(false);  const [loading, setLoading] = useState(false);  const options = [    ...(revertablePlanName      ? [{          key: 'revert' as const,          icon: <RotateCcw size={20} />,          title: isTamil ? `${revertablePlanName} திட்டத்திற்கு மாற்றவும்` : `Revert to ${revertablePlanName}`,          desc: isTamil            ? 'முந்தைய திட்டம் இன்னும் காலாவதியாகவில்லை. இதற்கு மாற்றலாம்.'            : 'Your previous plan is still valid. Switch back to it.',          color: 'border-amber-200 hover:border-amber-400 bg-amber-50/30',          activeColor: 'border-amber-500 bg-amber-50 ring-2 ring-amber-200',        }]      : []),    {      key: 'cancel' as const,      icon: <XCircle size={20} />,      title: isTamil ? 'இலவச (வெண்கல) திட்டத்திற்கு மாற்றவும்' : 'Cancel to Free (BRONZE)',      desc: isTamil        ? 'தற்போதைய திட்டத்தை ரத்துசெய்து இலவச திட்டத்திற்கு மாற்றவும்.'        : 'Cancel the current plan and revert to the free BRONZE plan.',      color: 'border-slate-200 hover:border-slate-400 bg-white',      activeColor: 'border-slate-500 bg-slate-50 ring-2 ring-slate-200',    },  ];  const handleContinue = async () => {    if (!selectedAction || loading) return;    if (!confirming) { setConfirming(true); return; }    setLoading(true);    try {      await onConfirm(selectedAction);    } finally {      setLoading(false);      setConfirming(false);      setSelectedAction(null);    }  };  const handleClose = () => {    setSelectedAction(null);    setConfirming(false);    setLoading(false);    onClose();  };  return (    <ModalShell      isOpen={isOpen}      onClose={handleClose}      title={isTamil ? 'சந்தாவை ரத்துசெய்' : 'Cancel Subscription'}      icon={<AlertTriangle size={16} />}      size="md"    >      <div className="space-y-4">        <p className="text-sm text-slate-600">          {isTamil            ? 'தற்போதைய திட்டத்தை ரத்துசெய்ய விரும்புகிறீர்களா? கீழே உள்ள விருப்பங்களில் ஒன்றைத் தேர்ந்தெடுக்கவும்.'            : `The account currently has the ${currentPlanName} plan. Choose how to proceed:`}        </p>        <div className="space-y-3">          {options.map((opt) => (            <motion.button              key={opt.key}              whileHover={{ scale: 1.01 }}              whileTap={{ scale: 0.99 }}              onClick={() => { setSelectedAction(opt.key); setConfirming(false); }}              className={`w-full text-left p-4 rounded-xl border transition-all duration-200 ${                selectedAction === opt.key ? opt.activeColor : opt.color              }`}            >              <div className="flex items-start gap-3">                <span className={`mt-0.5 ${selectedAction === opt.key ? 'text-amber-600' : 'text-slate-400'}`}>                  {opt.icon}                </span>                <div>                  <p className="text-sm font-bold text-slate-800">{opt.title}</p>                  <p className="text-xs text-slate-500 mt-0.5">{opt.desc}</p>                </div>                <div className={`ml-auto w-5 h-5 rounded-full border-2 shrink-0 flex items-center justify-center transition-all ${                  selectedAction === opt.key                    ? 'border-amber-500 bg-amber-500'                    : 'border-slate-300'                }`}>                  {selectedAction === opt.key && (                    <span className="w-2 h-2 rounded-full bg-white" />                  )}                </div>              </div>            </motion.button>          ))}        </div>        <AnimatePresence mode="wait">          {confirming && (            <motion.div              initial={{ opacity: 0, y: -8 }}              animate={{ opacity: 1, y: 0 }}              exit={{ opacity: 0, y: -8 }}              className="p-3 rounded-lg bg-red-50 border border-red-200"            >              <p className="text-xs text-red-700 font-medium">                {isTamil                  ? 'இதை உறுதிசெய்கிறீர்களா? இந்த மாற்றத்தை பின்னர் மாற்ற முடியாது.'                  : 'Are you sure? This action will change the active subscription immediately.'}              </p>            </motion.div>          )}        </AnimatePresence>        <div className="flex gap-3 pt-2">          <motion.button            whileHover={{ y: -1 }}            whileTap={{ y: 0 }}            onClick={handleClose}            className="flex-1 rounded-xl border border-slate-200 bg-white text-slate-700 font-bold text-sm px-4 py-2.5 hover:shadow-sm transition-all"          >            {isTamil ? 'ரத்துசெய்' : 'Cancel'}          </motion.button>          <motion.button            whileHover={{ y: -1 }}            whileTap={{ y: 0 }}            onClick={handleContinue}            disabled={!selectedAction || loading}            className="flex-1 rounded-xl bg-rosewood-gradient text-white font-bold text-sm px-4 py-2.5 disabled:opacity-50 disabled:cursor-not-allowed hover:shadow-md transition-all flex items-center justify-center gap-2"          >            {loading && <Loader2 size={15} className="animate-spin" />}            {loading              ? (isTamil ? 'மாற்றுகிறது...' : 'Processing...')              : confirming                ? (isTamil ? 'ஆமாம், உறுதிசெய்கிறேன்' : 'Yes, Confirm')                : (isTamil ? 'தொடரவும்' : 'Continue')}          </motion.button>        </div>      </div>    </ModalShell>  );};export default CancelSubscriptionModal;