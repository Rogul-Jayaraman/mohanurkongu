import React, { useMemo, useState } from 'react';import { useNavigate } from 'react-router-dom';import { useLanguage } from '@/context/LanguageContext';import { Eye, User as UserIcon, Filter, ChevronDown, ArrowUpDown } from 'lucide-react';import { StatusBadge } from '@/components/ui/feedback/StatusBadge';import { AdminPageLayout } from '@/components/ui/layout/AdminPageLayout';import { ModalShell } from '@/components/ui/modals/ModalShell';import { DataTable, Column } from '@/components/ui/table/DataTable';import { PROFILE_STATUS_FILTER_TABS } from '@/constants/admin/profileStatus';import { useDateFormatter } from '@/hooks/useDateFormatter';import type { AdminManagedProfile } from '@/types/admin-types';import { useAdminProfilesQuery } from '@/queries/useProfileQueries';import { Tooltip } from '@/components/ui/Tooltip';import { getImageUrl } from '@/utils/getImageUrl';const GENDER_OPTIONS = [  { value: '', label: 'All Genders' },  { value: 'MALE', label: 'Male' },  { value: 'FEMALE', label: 'Female' },  { value: 'OTHER', label: 'Other' },];const SORT_OPTIONS = [  { value: 'createdAt', label: 'Created Date' },  { value: 'updatedAt', label: 'Updated Date' },  { value: 'regNo', label: 'Registration No' },];// ═══════════════════════════════════════════════════════════// ProfileManagement (Main Orchestrator)// ═══════════════════════════════════════════════════════════const ProfileManagement: React.FC = () => {    const { language, t } = useLanguage();    const { formatDate } = useDateFormatter();    const navigate = useNavigate();    const isTamil = language === 'ta';    const [currentPage, setCurrentPage] = React.useState(1);    const itemsPerPage = 8;    const [searchQuery, setSearchQuery] = React.useState('');    const [statusFilter, setStatusFilter] = React.useState<string>('All');    const [genderFilter, setGenderFilter] = React.useState('');    const [dateFrom, setDateFrom] = React.useState('');    const [dateTo, setDateTo] = React.useState('');    const [sortBy, setSortBy] = React.useState('createdAt');    const [sortOrder, setSortOrder] = React.useState('desc');    const [drawerOpen, setDrawerOpen] = useState(false);    const filters = useMemo(() => {      const f: Record<string, unknown> = { page: currentPage, limit: itemsPerPage };      if (statusFilter !== 'All') f.status = statusFilter;      if (searchQuery) f.search = searchQuery;      if (genderFilter) f.gender = genderFilter;      if (dateFrom) f.createdAtFrom = dateFrom;      if (dateTo) f.createdAtTo = dateTo;      if (sortBy !== 'createdAt') f.sortBy = sortBy;      if (sortOrder !== 'desc') f.sortOrder = sortOrder;      return f;    }, [currentPage, statusFilter, searchQuery, genderFilter, dateFrom, dateTo, sortBy, sortOrder]);    const adminProfilesQuery = useAdminProfilesQuery(filters);    const data: any = (adminProfilesQuery.data as any) ?? { profiles: [], meta: { total: 0, totalPages: 1, page: 1, limit: itemsPerPage } };    const isLoading = adminProfilesQuery.isPending;    const columns: Column<AdminManagedProfile>[] = [        {            header: t('adminMatrimony.profiles.table.profile') || 'Matrimony Profile',            render: (profile) => {                const photoValue = profile.profilePhoto || profile.photo || (profile.photos && profile.photos[0]);                const rawUrl = typeof photoValue === 'string' ? photoValue : photoValue?.url || null;                const imageUrl = getImageUrl(rawUrl);                const displayName = isTamil ? ([profile.firstNameTa, profile.lastNameTa].filter(Boolean).join(' ') || [profile.firstNameEn, profile.lastNameEn].filter(Boolean).join(' ')) : ([profile.firstNameEn, profile.lastNameEn].filter(Boolean).join(' '));                return (                    <div className="flex items-center gap-3">                        <div className="relative w-12 h-14 rounded-lg overflow-hidden shrink-0 border border-gold/20 bg-ivory shadow-sm group-hover:scale-105 transition-transform duration-300">                            {imageUrl ? <img src={imageUrl} alt={displayName} className="w-full h-full object-cover" loading="lazy" decoding="async" /> : <div className="w-full h-full flex items-center justify-center text-rosewood bg-gold/10 text-xl font-serif font-bold">{displayName.charAt(0).toUpperCase()}</div>}                        </div>                        <div>                            <div className="font-bold text-rosewood text-sm leading-tight mb-0.5">{displayName}</div>                            <span className="text-[10px] text-gold font-bold">{profile.regNo}</span>                        </div>                    </div>                );            }        },        {            header: t('adminMatrimony.profiles.table.owner') || 'Account Owner',            render: (profile) => (                <div className="flex items-center gap-2">                    <div className="p-1.5 rounded-lg bg-rosewood/5 text-rosewood"><UserIcon size={12} /></div>                    <div>                        <div className="text-xs font-bold text-rosewood leading-none mb-1">{isTamil ? [profile.owner.firstNameTa, profile.owner.lastNameTa].filter(Boolean).join(' ') : [profile.owner.firstNameEn, profile.owner.lastNameEn].filter(Boolean).join(' ')}</div>                        <span className="text-[9px] text-slate-400 font-bold">ID: {profile.owner.id}</span>                    </div>                </div>            )        },        {            header: t('adminMatrimony.profiles.table.created') || 'Created',            render: (profile) => <div className="text-xs font-bold text-slate-500 tabular-nums">{formatDate(profile.createdAt)}</div>        },        {            header: t('adminMatrimony.profiles.table.status') || 'Verification',            render: (profile) => <StatusBadge status={(profile.status || '').toLowerCase()} minimal />        },        {            header: t('adminMatrimony.common.actions') || 'Actions',            headerClassName: 'w-16 text-center',            className: 'text-center',            render: (profile) => (                <div className="flex justify-center">                    <Tooltip content={t('adminMatrimony.verification.viewProfile') || 'View Profile'}>                        <button                             onClick={() => navigate(`/admin/matrimony/profiles/${profile.id}`)}                            className="p-2.5 rounded-xl bg-gold/5 text-gold hover:bg-rosewood hover:text-white transition-all duration-300 shadow-sm border border-gold/10 hover:border-rosewood"                        >                            <Eye size={18} strokeWidth={2.5} />                        </button>                    </Tooltip>                </div>            )        }    ];    const handleClearFilters = () => {      setSearchQuery('');      setStatusFilter('All');      setGenderFilter('');      setDateFrom('');      setDateTo('');      setSortBy('createdAt');      setSortOrder('desc');      setCurrentPage(1);    };    const handleSearchQuery = (v: string) => {      setSearchQuery(v);      setCurrentPage(1);    };    const hasActiveFilters = genderFilter || dateFrom || dateTo || sortBy !== 'createdAt' || sortOrder !== 'desc';    return (        <AdminPageLayout            searchQuery={searchQuery}            onSearchChange={handleSearchQuery}            searchPlaceholder={t('adminMatrimony.profiles.searchPlaceholder') || 'Search by name, reg no...'}            statusTabs={PROFILE_STATUS_FILTER_TABS}            activeStatus={statusFilter}            onStatusChange={(v) => { setStatusFilter(v); setCurrentPage(1); }}            hasActiveFilters={hasActiveFilters}            onClearFilters={handleClearFilters}            filterButton={                <>                    <button                        onClick={() => setDrawerOpen(true)}                        className={`inline-flex items-center gap-2 px-4 py-3 rounded-xl border text-xs font-bold transition-all active:scale-95 shrink-0 ${                            hasActiveFilters                                ? 'bg-rosewood-gradient text-ivory border-rosewood shadow-md shadow-rosewood/20'                                : 'bg-white/60 text-rosewood/60 border-gold/10 hover:border-gold/30 hover:text-rosewood/80'                        }`}                    >                        <Filter size={14} />                        {t('adminMatrimony.common.filters') || 'Filters'}                        {hasActiveFilters && <span className="w-2 h-2 rounded-full bg-white/80" />}                    </button>                    <ModalShell                        isOpen={drawerOpen}                        onClose={() => setDrawerOpen(false)}                        icon={<Filter size={24} className="text-rosewood" />}                        title={t('adminMatrimony.common.filters') || 'Filters'}                        size="sm"                        footer={                            <div className="flex gap-3">                                <button                                    onClick={() => { handleClearFilters(); setDrawerOpen(false); }}                                    className="flex-1 px-6 py-3 border border-gold/20 text-rosewood font-bold rounded-xl hover:bg-ivory transition-all text-sm active:scale-[0.97]"                                >                                    {t('adminMatrimony.common.clearFilters') || 'Clear All Filters'}                                </button>                                <button                                    onClick={() => setDrawerOpen(false)}                                    className="flex-1 px-6 py-3 bg-rosewood-gradient text-ivory font-bold rounded-xl hover:shadow-xl transition-all text-sm shadow-lg shadow-rosewood/20 active:scale-[0.97]"                                >                                    {t('adminMatrimony.common.apply') || 'Apply Filters'}                                </button>                            </div>                        }                    >                        <div className="space-y-6">                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    {t('adminMatrimony.profiles.gender') || 'Gender'}                                </label>                                <div className="relative">                                    <select                                        value={genderFilter}                                        onChange={(e) => { setGenderFilter(e.target.value); setCurrentPage(1); }}                                        className="w-full appearance-none px-4 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-sm font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                    >                                        {GENDER_OPTIONS.map((o) => (                                            <option key={o.value} value={o.value}>{o.label}</option>                                        ))}                                    </select>                                    <ChevronDown size={14} className="absolute right-3 top-1/2 -translate-y-1/2 text-rosewood/30 pointer-events-none" />                                </div>                            </div>                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    {t('adminMatrimony.profiles.dateRange') || 'Date Range'}                                </label>                                <div className="grid grid-cols-2 gap-3">                                    <input                                        type="date"                                        value={dateFrom}                                        onChange={(e) => { setDateFrom(e.target.value); setCurrentPage(1); }}                                        className="w-full px-3 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-xs font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                    />                                    <input                                        type="date"                                        value={dateTo}                                        onChange={(e) => { setDateTo(e.target.value); setCurrentPage(1); }}                                        className="w-full px-3 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-xs font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                    />                                </div>                            </div>                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    {t('adminMatrimony.profiles.sortBy') || 'Sort By'}                                </label>                                <div className="relative">                                    <select                                        value={sortBy}                                        onChange={(e) => { setSortBy(e.target.value); setCurrentPage(1); }}                                        className="w-full appearance-none px-4 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-sm font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                    >                                        {SORT_OPTIONS.map((o) => (                                            <option key={o.value} value={o.value}>{o.label}</option>                                        ))}                                    </select>                                    <ChevronDown size={14} className="absolute right-3 top-1/2 -translate-y-1/2 text-rosewood/30 pointer-events-none" />                                </div>                            </div>                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    {t('adminMatrimony.profiles.sortOrder') || 'Sort Order'}                                </label>                                <div className="flex gap-2">                                    <button                                        onClick={() => { setSortOrder('asc'); setCurrentPage(1); }}                                        className={`flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-xl border text-xs font-bold transition-all active:scale-95 ${                                            sortOrder === 'asc'                                                ? 'bg-rosewood text-white border-rosewood shadow-md shadow-rosewood/20'                                                : 'bg-ivory/60 text-rosewood/50 border-gold/10 hover:border-gold/30 hover:text-rosewood/80'                                        }`}                                    >                                        <ArrowUpDown size={14} className="rotate-180" />                                        {t('adminMatrimony.common.asc') || 'Asc'}                                    </button>                                    <button                                        onClick={() => { setSortOrder('desc'); setCurrentPage(1); }}                                        className={`flex-1 flex items-center justify-center gap-2 px-4 py-2.5 rounded-xl border text-xs font-bold transition-all active:scale-95 ${                                            sortOrder === 'desc'                                                ? 'bg-rosewood text-white border-rosewood shadow-md shadow-rosewood/20'                                                : 'bg-ivory/60 text-rosewood/50 border-gold/10 hover:border-gold/30 hover:text-rosewood/80'                                        }`}                                    >                                        <ArrowUpDown size={14} />                                        {t('adminMatrimony.common.desc') || 'Desc'}                                    </button>                                </div>                            </div>                        </div>                    </ModalShell>                </>            }        >            <DataTable columns={columns} data={data?.profiles || []} loading={isLoading} pagination={{ currentPage, totalPages: data?.meta?.totalPages || 1, totalItems: data?.meta?.total || 0, itemsPerPage, onPageChange: setCurrentPage }} emptyState={{ icon: Eye, title: t('adminMatrimony.users.noProfilesFound') }} />        </AdminPageLayout>    );};export default ProfileManagement;