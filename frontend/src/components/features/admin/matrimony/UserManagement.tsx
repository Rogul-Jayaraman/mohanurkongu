import React, { useMemo, useState } from 'react';import { useNavigate } from 'react-router-dom';import { useLanguage } from '@/context/LanguageContext';import { Eye, Phone, Mail, Users as UsersIcon, UserX, Filter, ChevronDown } from 'lucide-react';import { Tooltip } from '@/components/ui/Tooltip';import { StatusBadge } from '@/components/ui/feedback/StatusBadge';import { DataTable, Column } from '@/components/ui/table/DataTable';import { AdminPageLayout } from '@/components/ui/layout/AdminPageLayout';import { ModalShell } from '@/components/ui/modals/ModalShell';import { useDateFormatter } from '@/hooks/useDateFormatter';import { useAdminAccountsQuery } from '@/queries/useAdminAccountQueries';const ACCOUNT_STATUS_TABS = [  { value: 'All', label: 'All', icon: UsersIcon, color: 'text-rosewood/50 border-transparent hover:border-gold/30 hover:text-rosewood/80', active: 'bg-rosewood-gradient text-ivory border-rosewood shadow-md shadow-rosewood/20' },  { value: 'ACTIVE', label: 'Active', icon: UsersIcon, color: 'text-emerald-600/70 border-transparent hover:border-emerald-300 hover:text-emerald-700', active: 'bg-emerald-600 text-white border-emerald-600 shadow-md shadow-emerald-300/30' },  { value: 'SUSPENDED', label: 'Suspended', icon: UsersIcon, color: 'text-amber-600/70 border-transparent hover:border-amber-300 hover:text-amber-700', active: 'bg-amber-600 text-white border-amber-600 shadow-md shadow-amber-300/30' },];const PLAN_OPTIONS = [  { value: '', label: 'All Plans' },  { value: 'BRONZE', label: 'Bronze' },  { value: 'SILVER', label: 'Silver' },  { value: 'GOLD', label: 'Gold' },  { value: 'PLATINUM', label: 'Platinum' },];const VERIFIED_OPTIONS = [  { value: '', label: 'Any' },  { value: 'true', label: 'Yes' },  { value: 'false', label: 'No' },];const UserManagement: React.FC = () => {    const navigate = useNavigate();    const { t, language } = useLanguage();    const { formatDate } = useDateFormatter();    const isTamil = language === 'ta';    const [currentPage, setCurrentPage] = React.useState(1);    const [searchQuery, setSearchQuery] = React.useState('');    const [statusFilter, setStatusFilter] = React.useState('All');    const [planFilter, setPlanFilter] = React.useState('');    const [dateFrom, setDateFrom] = React.useState('');    const [dateTo, setDateTo] = React.useState('');    const [emailVerified, setEmailVerified] = React.useState('');    const [drawerOpen, setDrawerOpen] = useState(false);    const params = useMemo(() => ({        page: currentPage,        search: searchQuery,    }), [currentPage, searchQuery]);    const accountsQuery = useAdminAccountsQuery(params);    const rawData: any = (accountsQuery.data as any) ?? { accounts: [], meta: { total: 0, totalPages: 1, page: 1, limit: 8 } };    const isLoading = accountsQuery.isPending;    const filteredAccounts = useMemo(() => {        let list = rawData?.accounts || [];        if (statusFilter !== 'All') {            list = list.filter((a: any) => a.accountStatus === statusFilter);        }        if (planFilter) {            list = list.filter((a: any) => a.plan === planFilter);        }        if (emailVerified) {            const wantVerified = emailVerified === 'true';            list = list.filter((a: any) => a.emailVerified === wantVerified);        }        if (dateFrom) {            const from = new Date(dateFrom);            list = list.filter((a: any) => new Date(a.joinedDate) >= from);        }        if (dateTo) {            const to = new Date(dateTo);            to.setHours(23, 59, 59, 999);            list = list.filter((a: any) => new Date(a.joinedDate) <= to);        }        return list;    }, [rawData, statusFilter, planFilter, emailVerified, dateFrom, dateTo]);    const handleClearFilters = () => {        setStatusFilter('All');        setPlanFilter('');        setDateFrom('');        setDateTo('');        setEmailVerified('');        setCurrentPage(1);    };    const handleSearchQuery = (v: string) => {        setSearchQuery(v);        setCurrentPage(1);    };    const hasActiveFilters = planFilter || dateFrom || dateTo || emailVerified;    const columns: Column<any>[] = [        {            header: 'User',            render: (user) => (                <div>                    <div className="font-bold text-rosewood/80 text-sm">                        {isTamil                            ? [user.firstNameTa, user.lastNameTa].filter(Boolean).join(' ') ||                              [user.firstNameEn, user.lastNameEn].filter(Boolean).join(' ')                            : [user.firstNameEn, user.lastNameEn].filter(Boolean).join(' ')}                    </div>                    <div className="text-[10px] text-gold font-bold mt-0.5">{user.customId}</div>                </div>            )        },        {            header: 'Contact',            render: (user) => (                <div className="flex flex-col gap-0.5">                    <div className="flex items-center gap-1.5 text-xs text-slate-600">                        <Phone size={11} className="text-gold shrink-0" />                        <span>{user.phone}</span>                    </div>                    <div className="flex items-center gap-1.5 text-[11px] text-slate-400">                        <Mail size={10} className="text-gold/60 shrink-0" />                        <span className="truncate max-w-[180px]">{user.email}</span>                    </div>                </div>            )        },        {            header: 'Profiles',            render: (user) => (                <div className="flex items-center gap-2">                    <div className="w-8 h-8 rounded-lg bg-ivory flex items-center justify-center border border-gold/10 text-rosewood">                        <UsersIcon size={14} />                    </div>                    <div>                        <div className="text-xs font-bold text-rosewood">{user.profileCount}</div>                        <div className="text-[9px] text-slate-400 font-bold">Profiles</div>                    </div>                </div>            )        },        {            header: 'Joined',            render: (user) => (                <div className="text-xs font-bold text-slate-500 tabular-nums">                    {formatDate(user.joinedDate)}                </div>            )        },        {            header: 'Account Status',            render: (user) => (                <StatusBadge status={(user.accountStatus || '').toLowerCase()} minimal />            )        },        {            header: 'Actions',            headerClassName: 'w-16 text-center',            className: 'text-center',            render: (user) => (                <div className="flex justify-center">                    <Tooltip content="View Details">                        <button                            onClick={() => navigate(`/admin/matrimony/account/${user.id}`)}                            className="p-2.5 rounded-xl bg-gold/5 text-gold hover:bg-rosewood hover:text-white transition-all duration-300 shadow-sm border border-gold/10 hover:border-rosewood"                        >                            <Eye size={18} strokeWidth={2.5} />                        </button>                    </Tooltip>                </div>            )        }    ];    return (        <AdminPageLayout            searchQuery={searchQuery}            onSearchChange={handleSearchQuery}            searchPlaceholder="Search by name, ID, phone or email..."            statusTabs={ACCOUNT_STATUS_TABS}            activeStatus={statusFilter}            onStatusChange={(v) => { setStatusFilter(v); setCurrentPage(1); }}            hasActiveFilters={hasActiveFilters}            onClearFilters={handleClearFilters}            filterButton={                <>                    <button                        onClick={() => setDrawerOpen(true)}                        className={`inline-flex items-center gap-2 px-4 py-3 rounded-xl border text-xs font-bold transition-all active:scale-95 shrink-0 ${                            hasActiveFilters                                ? 'bg-rosewood-gradient text-ivory border-rosewood shadow-md shadow-rosewood/20'                                : 'bg-white/60 text-rosewood/60 border-gold/10 hover:border-gold/30 hover:text-rosewood/80'                        }`}                    >                        <Filter size={14} />                        Filters                        {hasActiveFilters && <span className="w-2 h-2 rounded-full bg-white/80" />}                    </button>                    <ModalShell                        isOpen={drawerOpen}                        onClose={() => setDrawerOpen(false)}                        icon={<Filter size={24} className="text-rosewood" />}                        title="Filters"                        size="sm"                        footer={                            <div className="flex gap-3">                                <button                                    onClick={() => { handleClearFilters(); setDrawerOpen(false); }}                                    className="flex-1 px-6 py-3 border border-gold/20 text-rosewood font-bold rounded-xl hover:bg-ivory transition-all text-sm active:scale-[0.97]"                                >                                    Clear All Filters                                </button>                                <button                                    onClick={() => setDrawerOpen(false)}                                    className="flex-1 px-6 py-3 bg-rosewood-gradient text-ivory font-bold rounded-xl hover:shadow-xl transition-all text-sm shadow-lg shadow-rosewood/20 active:scale-[0.97]"                                >                                    Apply Filters                                </button>                            </div>                        }                    >                        <div className="space-y-6">                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    Membership Plan                                </label>                                <div className="relative">                                    <select                                        value={planFilter}                                        onChange={(e) => { setPlanFilter(e.target.value); setCurrentPage(1); }}                                        className="w-full appearance-none px-4 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-sm font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                    >                                        {PLAN_OPTIONS.map((o) => (                                            <option key={o.value} value={o.value}>{o.label}</option>                                        ))}                                    </select>                                    <ChevronDown size={14} className="absolute right-3 top-1/2 -translate-y-1/2 text-rosewood/30 pointer-events-none" />                                </div>                            </div>                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    Date Range (Joined)                                </label>                                <div className="grid grid-cols-2 gap-3">                                    <div className="relative">                                        <input                                            type="date"                                            value={dateFrom}                                            onChange={(e) => { setDateFrom(e.target.value); setCurrentPage(1); }}                                            className="w-full px-3 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-xs font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                        />                                    </div>                                    <div className="relative">                                        <input                                            type="date"                                            value={dateTo}                                            onChange={(e) => { setDateTo(e.target.value); setCurrentPage(1); }}                                            className="w-full px-3 py-2.5 bg-ivory/60 border border-gold/10 rounded-xl text-xs font-medium text-rosewood focus:outline-none focus:border-gold/40 focus:ring-2 focus:ring-gold/5 transition-all"                                        />                                    </div>                                </div>                                <div className="flex text-[10px] font-medium text-rosewood/30 justify-between px-1">                                    <span>From</span>                                    <span>To</span>                                </div>                            </div>                            <div className="space-y-2">                                <label className="text-[11px] font-bold text-rosewood/50 uppercase tracking-wider">                                    Email Verified                                </label>                                <div className="flex gap-2">                                    {VERIFIED_OPTIONS.map((opt) => (                                        <button                                            key={opt.value}                                            onClick={() => { setEmailVerified(opt.value); setCurrentPage(1); }}                                            className={`flex-1 px-4 py-2.5 rounded-xl border text-xs font-bold transition-all active:scale-95 ${                                                emailVerified === opt.value                                                    ? 'bg-rosewood text-white border-rosewood shadow-md shadow-rosewood/20'                                                    : 'bg-ivory/60 text-rosewood/50 border-gold/10 hover:border-gold/30 hover:text-rosewood/80'                                            }`}                                        >                                            {opt.label}                                        </button>                                    ))}                                </div>                            </div>                        </div>                    </ModalShell>                </>            }        >            <DataTable                columns={columns}                data={filteredAccounts}                loading={isLoading}                pagination={{                    currentPage,                    totalPages: rawData?.meta?.totalPages || 1,                    totalItems: rawData?.meta?.total || 0,                    itemsPerPage: 8,                    onPageChange: setCurrentPage,                }}                emptyState={{                    icon: UserX,                    title: 'No accounts found matching your search.',                }}            />        </AdminPageLayout>    );};export default UserManagement;