import React, { useState, useEffect } from 'react';import { useLanguage } from '@/context/LanguageContext';import { ModalShell } from '@/components/ui/modals/ModalShell';import { Loader2, Plus, X, Save } from 'lucide-react';import { useUpdatePackage } from '@/queries/useMandapamMutations';import { toast } from 'sonner';import { formatCurrency } from '@/utils/format';import TranslatableInput from '@/components/ui/forms/TranslatableInput';import type { MandapamPackage, TranslationPair } from '@/types/mandapam';interface EditPackageModalProps {    isOpen: boolean;    onClose: () => void;    pkg: MandapamPackage;    onSuccess: () => void;}export const EditPackageModal: React.FC<EditPackageModalProps> = ({ isOpen, onClose, pkg, onSuccess }) => {    const { language } = useLanguage();    const isTamil = language === 'ta';    const updatePackage = useUpdatePackage();    const isSaving = updatePackage.isPending;    const [removedFunctionIds, setRemovedFunctionIds] = useState<Set<string>>(new Set());    const [newFunctions, setNewFunctions] = useState<{ enName: string; taName: string }[]>([]);    const [newFnEn, setNewFnEn] = useState('');    const [newFnTa, setNewFnTa] = useState('');    const [newPrice, setNewPrice] = useState<number | ''>('');    useEffect(() => {        if (isOpen) {            setRemovedFunctionIds(new Set());            setNewFunctions([]);            setNewFnEn('');            setNewFnTa('');            setNewPrice('');        }    }, [isOpen, pkg]);    const handleRemoveFunction = (fnId: string) => {        setRemovedFunctionIds(prev => new Set(prev).add(fnId));    };    const handleUndoRemoveFunction = (fnId: string) => {        setRemovedFunctionIds(prev => {            const next = new Set(prev);            next.delete(fnId);            return next;        });    };    const handleAddFunction = () => {        if (!newFnEn.trim() || !newFnTa.trim()) return;        setNewFunctions(prev => [...prev, { enName: newFnEn.trim(), taName: newFnTa.trim() }]);        setNewFnEn('');        setNewFnTa('');    };    const handleRemoveNewFunction = (index: number) => {        setNewFunctions(prev => prev.filter((_, i) => i !== index));    };    const handleSave = async () => {        const functionsDto: { id?: string; name: TranslationPair[]; status?: boolean }[] = [];        pkg.functions.forEach(fn => {            if (removedFunctionIds.has(fn.id)) {                functionsDto.push({ id: fn.id, name: [], status: false });            }        });        newFunctions.forEach(fn => {            functionsDto.push({                name: [                    { language: 'EN', value: fn.enName },                    { language: 'TA', value: fn.taName },                ],            });        });        const payload: Record<string, any> = {};        if (functionsDto.length > 0) payload.functions = functionsDto;        if (newPrice !== '') payload.pricing = { amount: Number(newPrice) };        await updatePackage.mutateAsync({ id: pkg.id, dto: payload });        onSuccess();        onClose();    };    const activeFunctions = pkg.functions.filter(fn => fn.status && !removedFunctionIds.has(fn.id));    const footer = (        <div className="flex items-center justify-end gap-3">            <button                onClick={onClose}                className="px-6 py-2.5 text-sm font-black text-rosewood bg-white border-2 border-rosewood/10 rounded-2xl hover:bg-rosewood/5 transition-all uppercase tracking-wider"            >                {isTamil ? 'ரத்துசெய்' : 'Cancel'}            </button>            <button                onClick={handleSave}                disabled={isSaving}                className="px-6 py-2.5 text-sm font-black text-ivory bg-rosewood rounded-2xl hover:bg-rosewood-dark transition-all disabled:opacity-50 flex items-center gap-2 uppercase tracking-wider"            >                {isSaving ? <Loader2 size={16} className="animate-spin" /> : <Save size={16} />}                {isTamil ? 'சேமி' : 'Save'}            </button>        </div>    );    return (        <ModalShell            isOpen={isOpen}            onClose={onClose}            title={`${isTamil ? 'தொகுப்பைத் திருத்து' : 'Edit Package'} — ${pkg.code}`}            size="lg"            footer={footer}        >            <div className="space-y-6">                {/* Existing Functions */}                <div>                    <h4 className="text-[11px] font-black text-rosewood/30 uppercase tracking-[0.2em] mb-4">                        {isTamil ? 'தற்போதைய செயல்பாடுகள்' : 'Current Functions'}                    </h4>                    {activeFunctions.length === 0 && newFunctions.length === 0 && (                        <p className="text-sm text-rosewood/30 font-bold mb-4">                            {isTamil ? 'செயல்பாடுகள் எதுவும் இல்லை' : 'No functions available'}                        </p>                    )}                    <div className="space-y-2 mb-4">                        {activeFunctions.map((fn) => {                            const fnEn = fn.translations.find(tr => tr.language === 'EN')?.name ?? '';                            const fnTa = fn.translations.find(tr => tr.language === 'TA')?.name ?? '';                            return (                                <div key={fn.id} className="flex items-center gap-3 px-4 py-3 bg-white border border-rosewood/5 rounded-xl">                                    <div className="flex-1 grid grid-cols-1 sm:grid-cols-2 gap-3">                                        <input                                            type="text"                                            value={fnEn}                                            readOnly                                            className="w-full px-3 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm text-slate-500 font-medium"                                        />                                        <input                                            type="text"                                            value={fnTa}                                            readOnly                                            className="w-full px-3 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm text-slate-500 font-medium"                                        />                                    </div>                                    <button                                        onClick={() => handleRemoveFunction(fn.id)}                                        className="p-2 text-red-400 hover:bg-red-50 rounded-lg transition-all shrink-0"                                        title={isTamil ? 'அகற்று' : 'Remove'}                                    >                                        <X size={14} />                                    </button>                                </div>                            );                        })}                        {newFunctions.map((fn, index) => (                            <div key={`new-${index}`} className="flex items-center gap-3 px-4 py-3 bg-emerald-50 border border-emerald-200 rounded-xl">                                <div className="flex-1 grid grid-cols-1 sm:grid-cols-2 gap-3">                                    <input                                        type="text"                                        value={fn.enName}                                        readOnly                                        className="w-full px-3 py-2 bg-white border border-emerald-200 rounded-lg text-sm text-emerald-700 font-medium"                                    />                                    <input                                        type="text"                                        value={fn.taName}                                        readOnly                                        className="w-full px-3 py-2 bg-white border border-emerald-200 rounded-lg text-sm text-emerald-700 font-medium"                                    />                                </div>                                <button                                    onClick={() => handleRemoveNewFunction(index)}                                    className="p-2 text-red-400 hover:bg-red-50 rounded-lg transition-all shrink-0"                                    title={isTamil ? 'நீக்கு' : 'Delete'}                                >                                    <X size={14} />                                </button>                            </div>                        ))}                    </div>                    {removedFunctionIds.size > 0 && (                        <div className="space-y-2 mb-4">                            <p className="text-[10px] font-bold text-red-400 uppercase tracking-wider mb-2">                                {isTamil ? 'அகற்றப்படவுள்ளவை' : 'Marked for Removal'}                            </p>                            {pkg.functions.filter(fn => removedFunctionIds.has(fn.id)).map((fn) => {                                const fnEn = fn.translations.find(tr => tr.language === 'EN')?.name ?? '';                                const fnTa = fn.translations.find(tr => tr.language === 'TA')?.name ?? '';                                return (                                    <div key={fn.id} className="flex items-center gap-3 px-4 py-3 bg-red-50 border border-red-200 rounded-xl">                                        <div className="flex-1 grid grid-cols-1 sm:grid-cols-2 gap-3">                                            <input                                                type="text"                                                value={fnEn}                                                readOnly                                                className="w-full px-3 py-2 bg-red-50 border border-red-200 rounded-lg text-sm text-red-500 font-medium line-through"                                            />                                            <input                                                type="text"                                                value={fnTa}                                                readOnly                                                className="w-full px-3 py-2 bg-red-50 border border-red-200 rounded-lg text-sm text-red-500 font-medium line-through"                                            />                                        </div>                                        <button                                            onClick={() => handleUndoRemoveFunction(fn.id)}                                            className="px-3 py-1.5 text-[10px] font-black text-rosewood bg-white border border-rosewood/20 rounded-lg hover:bg-rosewood/5 transition-all uppercase tracking-wider shrink-0"                                        >                                            {isTamil ? 'மீளமை' : 'Undo'}                                        </button>                                    </div>                                );                            })}                        </div>                    )}                </div>                {/* Pricing */}                <div>                    <h4 className="text-[11px] font-black text-rosewood/30 uppercase tracking-[0.2em] mb-4">                        {isTamil ? 'விலை நிர்ணயம்' : 'Pricing'}                    </h4>                    <div className="p-4 bg-ivory rounded-2xl border border-gold/10 space-y-3">                        {pkg.pricings.filter(p => p.isActive).length > 0 && (                            <div className="flex items-center gap-2 text-xs text-rosewood/50 font-bold mb-2">                                <span>{isTamil ? 'தற்போதைய விலை:' : 'Current price:'}</span>                                <span className="text-rosewood font-black">{formatCurrency(pkg.pricings.filter(p => p.isActive)[0].amount)}</span>                                <span className="text-rosewood/30">({pkg.pricings.filter(p => p.isActive)[0].pricingType})</span>                            </div>                        )}                        <div className="space-y-1.5">                            <label className="text-[10px] font-bold text-rosewood/50 uppercase tracking-wider">                                {isTamil ? 'புதிய விலை (₹)' : 'New Price (₹)'}                            </label>                            <input                                type="number"                                min={0}                                step={0.01}                                placeholder={isTamil ? 'புதிய தொகையை உள்ளிடுக' : 'Enter new amount'}                                value={newPrice}                                onChange={(e) => setNewPrice(e.target.value === '' ? '' : Number(e.target.value))}                                className="w-full px-4 py-3 bg-white border border-rosewood/10 rounded-xl text-sm text-rosewood font-bold focus:border-gold focus:ring-2 focus:ring-gold/10 outline-none transition-all"                            />                        </div>                    </div>                </div>                {/* Add New Function */}                <div>                    <h4 className="text-[11px] font-black text-rosewood/30 uppercase tracking-[0.2em] mb-4">                        {isTamil ? 'புதிய செயல்பாட்டைச் சேர்' : 'Add New Function'}                    </h4>                    <div className="p-4 bg-ivory rounded-2xl border border-gold/10 space-y-3">                        <TranslatableInput                            label={isTamil ? 'செயல்பாட்டின் பெயர்' : 'Function Name'}                            valueEn={newFnEn}                            valueTa={newFnTa}                            onChangeEn={setNewFnEn}                            onChangeTa={setNewFnTa}                            placeholder={isTamil ? 'செயல்பாட்டின் பெயரை உள்ளிடுக' : 'Enter function name'}                        />                        <button                            onClick={handleAddFunction}                            disabled={!newFnEn.trim() || !newFnTa.trim()}                            className="flex items-center gap-2 px-4 py-2.5 text-[10px] font-black text-rosewood bg-white border-2 border-rosewood/10 rounded-xl hover:bg-rosewood hover:text-ivory hover:border-rosewood transition-all disabled:opacity-40 uppercase tracking-wider"                        >                            <Plus size={14} />                            {isTamil ? 'சேர்' : 'Add'}                        </button>                    </div>                </div>            </div>        </ModalShell>    );};