import React from 'react';import { motion } from 'framer-motion';import { Check, IndianRupee, Eye, Search, Printer, Star, Users, Crown } from 'lucide-react';import { useTranslation } from 'react-i18next';import type { MembershipPlan } from '@/api/membership.api';import { formatCurrency } from '@/utils/format';interface UserPlanCardProps {  plan: MembershipPlan;  currentPlanCode?: string;  onUpgrade?: (plan: MembershipPlan) => void;}const planColors: Record<string, string> = {  BRONZE: 'from-amber-700/10 to-amber-900/5 border-amber-700/20',  SILVER: 'from-slate-300/20 to-slate-400/10 border-slate-400/20',  GOLD: 'from-yellow-400/15 to-yellow-600/8 border-yellow-500/20',  PLATINUM: 'from-cyan-200/20 to-cyan-400/10 border-cyan-300/20',};const planAccent: Record<string, string> = {  BRONZE: 'bg-amber-700 text-white',  SILVER: 'bg-slate-500 text-white',  GOLD: 'bg-yellow-500 text-rosewood',  PLATINUM: 'bg-cyan-500 text-white',};export const UserPlanCard: React.FC<UserPlanCardProps> = ({ plan, currentPlanCode, onUpgrade }) => {  const { t } = useTranslation();  const isCurrent = plan.code === currentPlanCode;  const features = [    {      icon: <Eye size={14} />,      label: plan.openLimit < 0 ? t('myaccount:membership.card.unlimited') : t('myaccount:membership.card.opens', { count: plan.openLimit }),    },    {      icon: <Search size={14} />,      label: t('myaccount:membership.card.search', { level: plan.searchLevel }),    },    {      icon: <Users size={14} />,      label: t('myaccount:membership.card.profiles', { count: plan.profileSlotLimit }),    },    {      icon: <Star size={14} />,      label: plan.shortlistLimit < 0        ? t('myaccount:membership.card.shortlist_unlimited')        : plan.shortlistLimit === 0          ? t('myaccount:membership.card.shortlist_none')          : t('myaccount:membership.card.shortlist', { count: plan.shortlistLimit }),    },    {      icon: <Printer size={14} />,      label: plan.printProfile        ? t('myaccount:membership.card.print')        : t('myaccount:membership.card.no_print'),    },  ];  return (    <motion.div      initial={{ opacity: 0, y: 20 }}      animate={{ opacity: 1, y: 0 }}      className={`relative flex flex-col rounded-2xl border-2 transition-all duration-500 overflow-hidden ${        isCurrent          ? 'border-gold shadow-xl shadow-gold/10 scale-[1.02]'          : 'border-rosewood/10 shadow-lg hover:shadow-xl hover:-translate-y-1'      } ${planColors[plan.code] || 'border-slate-200/50 bg-white'}`}    >      {isCurrent && (        <div className="absolute top-3 right-3 z-10">          <div className="px-3 py-1 rounded-full bg-gold text-rosewood text-[9px] font-black uppercase tracking-wider shadow-md">            {t('myaccount:membership.card.current')}          </div>        </div>      )}      <div className="absolute inset-0 bg-linear-to-br from-white/60 to-white/10 pointer-events-none" />      <div className="relative z-10 p-6 flex flex-col flex-1">        <div className="mb-4">          <div className={`w-12 h-12 rounded-xl flex items-center justify-center mb-3 ${planAccent[plan.code] || 'bg-rosewood text-white'}`}>            {plan.code === 'PLATINUM' ? <Crown size={22} /> : <Check size={22} />}          </div>          <h3 className="text-lg font-serif font-bold text-rosewood">{plan.displayName}</h3>          <div className="flex items-baseline gap-1 mt-1">            <IndianRupee size={14} className="text-gold" />            <span className="text-2xl font-black text-rosewood">              {plan.displayPrice === 0 ? t('myaccount:membership.card.free') : formatCurrency(plan.displayPrice)}            </span>            {plan.durationDays > 0 && (              <span className="text-xs text-rosewood/40 font-medium ml-1">                / {plan.durationDays} {t('myaccount:membership.card.days')}              </span>            )}          </div>        </div>        <div className="flex-1 space-y-3 mb-6">          {features.map((feat, i) => (            <div key={i} className="flex items-center gap-3">              <div className="w-8 h-8 rounded-lg bg-ivory flex items-center justify-center shrink-0 border border-gold/10">                <span className="text-gold">{feat.icon}</span>              </div>              <span className="text-xs font-semibold text-slate-700">{feat.label}</span>            </div>          ))}        </div>        <button          onClick={() => onUpgrade?.(plan)}          disabled={isCurrent}          className={`w-full py-3 rounded-xl text-xs font-black uppercase tracking-wider transition-all flex items-center justify-center gap-2 ${            isCurrent              ? 'bg-ivory text-slate-400 border border-gold/10 cursor-default'              : 'bg-rosewood text-white shadow-md shadow-rosewood/20 hover:shadow-lg hover:shadow-rosewood/30 hover:-translate-y-0.5 active:scale-95'          }`}        >          {isCurrent ? t('myaccount:membership.card.current_plan') : t('myaccount:membership.card.upgrade')}        </button>      </div>    </motion.div>  );};