import React, { useState, useEffect } from 'react';import { useTranslation } from 'react-i18next';const NAV_KEYS: { id: string; key: string }[] = [  { id: 'basic', key: 'basic' },  { id: 'personal', key: 'personal' },  { id: 'community', key: 'community' },  { id: 'professional', key: 'professional' },  { id: 'family', key: 'family' },  { id: 'assets', key: 'assets' },  { id: 'contact', key: 'contact' },  { id: 'partner-preference', key: 'partner_preference' },  { id: 'horoscope', key: 'horoscope' },  { id: 'gallery', key: 'gallery' },];const QuickNav: React.FC = () => {  const [activeSection, setActiveSection] = useState('');  const { t } = useTranslation(['common']);  useEffect(() => {    const observer = new IntersectionObserver(      (entries) => {        for (const entry of entries) {          if (entry.isIntersecting) {            const id = entry.target.id.replace('section-', '');            setActiveSection(id);          }        }      },      { rootMargin: '-80px 0px -60% 0px', threshold: 0 },    );    NAV_KEYS.forEach((s) => {      const el = document.getElementById(`section-${s.id}`);      if (el) observer.observe(el);    });    return () => observer.disconnect();  }, []);  return (    <div className="no-print relative">      <div className="flex items-center h-11 sm:h-12 overflow-x-auto overflow-y-hidden scrollbar-hide sm:overflow-visible">        {NAV_KEYS.map((s) => {          const isActive = activeSection === s.id;          return (            <a              key={s.id}              href={`#section-${s.id}`}              onClick={(e) => {                e.preventDefault();                document.getElementById(`section-${s.id}`)?.scrollIntoView({ behavior: 'smooth' });              }}              className={`h-full px-3 sm:px-5 flex items-center font-serif font-bold text-[11px] sm:text-sm whitespace-nowrap shrink-0 transition-colors duration-200 relative select-none ${                 isActive ? 'text-rosewood' : 'text-rosewood/60 hover:text-rosewood'               }`}            >              {t(`common:section_nav.${s.key}`)}              {isActive && (                <div className="absolute bottom-0 left-3 sm:left-5 right-3 sm:right-5 h-0.5 bg-gold rounded-t-full" />              )}            </a>          );        })}      </div>    </div>  );};export default QuickNav;