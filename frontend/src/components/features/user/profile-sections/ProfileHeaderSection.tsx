import React from 'react';import { Info } from 'lucide-react';import { useTranslation } from 'react-i18next';import { useProfileUtils } from '@/hooks/useProfileUtils';import { useDateFormatter } from '@/hooks/useDateFormatter';import { getImageUrl } from '@/utils/getImageUrl';import { GENDER_OPTIONS } from '@/constants/index';import { SectionCard3D, SectionHeaderRedesigned, DetailRow } from '@/components/features/matrimony/ProfileViewPrimitives';interface ProfileHeaderSectionProps {  profile: any;  isLoading: boolean;  isTamil: boolean;  isOwner: boolean;}const ProfileHeaderSection: React.FC<ProfileHeaderSectionProps> = ({ profile, isLoading, isTamil, isOwner }) => {  const { t: tCommon } = useTranslation(['common']);  const { t, getEnumLabel, getLocationLabel, calculateAge } = useProfileUtils();  const { formatDate } = useDateFormatter();  const name = profile    ? isTamil      ? [profile.firstNameTa, profile.lastNameTa].filter(Boolean).join(' ') || [profile.firstNameEn, profile.lastNameEn].filter(Boolean).join(' ')      : [profile.firstNameEn, profile.lastNameEn].filter(Boolean).join(' ') || [profile.firstNameTa, profile.lastNameTa].filter(Boolean).join(' ')    : '';  const currentLocation = profile    ? getLocationLabel(        profile.currentDistrictEn || profile.currentDistrict,        profile.currentTaluk || profile.currentCityEn,        profile.currentDistrictTa,        profile.currentTalukTa,        profile.currentCityEn,        profile.currentStateEn,        profile.currentCountryEn,        profile.currentCityTa,        profile.currentStateTa,        profile.currentCountryTa,      )    : '';  const profilePhotoUrl = profile?.profilePhoto    ? (typeof profile.profilePhoto === 'object' && profile.profilePhoto?.url        ? getImageUrl(profile.profilePhoto.url)        : getImageUrl(profile.profilePhoto as string)) || ''    : '';  const ageDisplay = profile?.dob ? `${calculateAge(profile.dob)} ${tCommon('yrs')}` : '';  const genderLabel = profile?.gender ? getEnumLabel(profile.gender, GENDER_OPTIONS) : '';  if (isLoading) {    return (      <div className="grid grid-cols-1 md:grid-cols-12 gap-6 sm:gap-8">        <div className="md:col-span-8">          <div className="bg-white rounded-xl shadow-sm border border-gold/20 p-6 md:p-8">            <div className="flex items-center gap-3 mb-6 pb-3 border-b border-gold/10">              <div className="w-10 h-10 rounded-xl bg-gold/10 animate-pulse" />              <div className="h-5 w-32 bg-gold/10 rounded animate-pulse" />            </div>            <div className="space-y-0">              {[...Array(6)].map((_, i) => (                <div key={i} className="flex flex-col sm:flex-row py-2 sm:py-1.5 border-b border-gold/10 last:border-0 sm:items-baseline gap-1 sm:gap-0">                  <div className="h-4 w-24 bg-gold/10 rounded animate-pulse sm:w-[160px]" />                  <span className="hidden sm:inline-block w-6 text-center" />                  <div className="h-4 w-3/4 bg-gold/10 rounded animate-pulse" />                </div>              ))}            </div>          </div>        </div>        <div className="md:col-span-4">          <div className="bg-white rounded-xl shadow-sm border border-gold/20 p-4">            <div className="w-full aspect-square rounded-xl bg-gold/10 animate-pulse" />          </div>        </div>      </div>    );  }  return (    <div className="grid grid-cols-1 md:grid-cols-12 gap-6 sm:gap-8">      <div className="md:col-span-8">        <SectionCard3D>          <SectionHeaderRedesigned            title={tCommon('basic_info')}            icon={<Info size={16} />}            gradient="bg-rosewood-gradient text-white"            isTamil={isTamil}          >            {(() => {              if (!isOwner) return null;              const a = profile.adminVerified?.toLowerCase();              const s = profile.status;              const statusTag = (gradient: string, label: string) => (                <span className={`${gradient} text-[9px] sm:text-[10px] font-bold rounded-full px-2 sm:px-2.5 py-0.5 sm:py-1 shadow-sm whitespace-nowrap`}>{label}</span>              );              if (s === 'BLOCKED') return statusTag('bg-rosewood-gradient text-white', tCommon('adminMatrimony:common.blocked'));              if (s === 'SUSPENDED') return statusTag('bg-rosewood-gradient text-white', tCommon('adminMatrimony:common.suspended'));              if (s === 'INACTIVE') return statusTag('bg-rosewood-gradient text-white', tCommon('profile.status.inactive'));              if (a === 'rejected') return statusTag('bg-rosewood-gradient text-white', tCommon('adminMatrimony:common.rejected'));              if (a === 'pending') return statusTag('bg-ivory-gold-gradient', tCommon('pending'));              if (a === 'accepted') return statusTag('bg-ivory-gold-gradient', tCommon('verified'));              return null;            })()}          </SectionHeaderRedesigned>          <div className="space-y-0">            <DetailRow label={t('profile_new:full_name')} value={name} />            <DetailRow label={tCommon('profile.reg_no')} value={profile?.regNo} />            <DetailRow label={t('profile_new:dob')} value={profile?.dob ? formatDate(profile.dob) : ''} />            <DetailRow label={tCommon('profile.age')} value={ageDisplay} />            <DetailRow label={t('profile_new:gender')} value={genderLabel} />            <DetailRow label={t('profile_new:current_location')} value={currentLocation} />          </div>        </SectionCard3D>      </div>      <div className="md:col-span-4">        <div className="bg-white p-3 sm:p-4 rounded-xl shadow-sm border border-gold/20 flex flex-col items-center">          <div className="w-full max-w-56 sm:max-w-none aspect-square rounded-xl overflow-hidden border-4 border-ivory bg-ivory">            {profilePhotoUrl ? (              <img                src={profilePhotoUrl}                alt={name}                className="w-full h-full object-cover"                loading="lazy"                decoding="async"              />            ) : (              <div className="w-full h-full bg-ivory flex items-center justify-center">                <span className="text-rosewood/30 text-4xl font-serif font-bold">{name?.charAt(0)}</span>              </div>            )}          </div>        </div>      </div>    </div>  );};export default ProfileHeaderSection;