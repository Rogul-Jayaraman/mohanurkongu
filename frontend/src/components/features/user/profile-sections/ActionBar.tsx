import React from 'react';import { Heart, Shield, Archive, Trash2, Edit } from 'lucide-react';import type { ViewerRole } from '@/hooks/useProfileView';interface ActionBarProps {  viewerRole: ViewerRole;  isShortlisted: boolean;  isOwner?: boolean;  onToggleShortlist?: () => void;  onVerify?: () => void;  onReject?: () => void;  onArchive?: () => void;  onDelete?: () => void;  onEdit?: () => void;}const ActionBar: React.FC<ActionBarProps> = ({  viewerRole,  isShortlisted,  onToggleShortlist,  onVerify,  onReject,  onArchive,  onDelete,  onEdit,}) => {  if (viewerRole === 'self') {    return (      <div className="flex gap-2">        {onEdit && (          <button onClick={onEdit} className="flex items-center gap-1.5 px-4 py-2 bg-rosewood text-white rounded-xl text-xs font-bold">            <Edit size={14} /> <span>Edit</span>          </button>        )}        {onDelete && (          <button onClick={onDelete} className="flex items-center gap-1.5 px-4 py-2 bg-red-500 text-white rounded-xl text-xs font-bold">            <Trash2 size={14} /> <span>Delete</span>          </button>        )}      </div>    );  }  if (viewerRole === 'admin') {    return (      <div className="flex gap-2">        {onArchive && (          <button onClick={onArchive} className="flex items-center gap-1.5 px-4 py-2 bg-slate-600 text-white rounded-xl text-xs font-bold">            <Archive size={14} /> <span>Archive</span>          </button>        )}        {onDelete && (          <button onClick={onDelete} className="flex items-center gap-1.5 px-4 py-2 bg-red-500 text-white rounded-xl text-xs font-bold">            <Trash2 size={14} /> <span>Delete</span>          </button>        )}      </div>    );  }  // public  return (    <div className="flex gap-2">      {onToggleShortlist && (        <button onClick={onToggleShortlist} className="flex items-center gap-1.5 px-4 py-2 border border-rosewood/20 rounded-xl text-xs font-bold">          <Heart size={14} className={isShortlisted ? 'fill-red-500 text-red-500' : ''} />          <span>{isShortlisted ? 'Shortlisted' : 'Shortlist'}</span>        </button>      )}    </div>  );};export default ActionBar;