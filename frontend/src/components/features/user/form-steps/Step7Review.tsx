import React from 'react';import { useTranslation } from 'react-i18next';import { getBilingualLabel } from '../../../../utils/bilingual';import { formatCurrency } from '@/utils/format';import { MediaImage } from '../../../ui/media/MediaImage';import { D1Chart, D9Chart } from '../../../shared/horoscope';import type { HoroscopeResult } from '@/types/horoscope';import {    NAKSHATRA_OPTIONS, RASI_OPTIONS, GENDER_OPTIONS, HEIGHT_OPTIONS, PROFILE_FOR_OPTIONS,    MARITAL_STATUS_OPTIONS, BLOOD_GROUP_OPTIONS, DIET_OPTIONS, COMPLEXION_OPTIONS,    RESIDENCE_OPTIONS, KULAM_OPTIONS, JOB_SECTOR_OPTIONS} from '../../../../constants';import { DISTRICT_TAMIL, TALUK_TAMIL } from '../../../../constants/locations';import type { StepProps } from './types';const Step7Review: React.FC<StepProps> = ({ formData, updateField, onAction, fieldErrors = {} }) => {    const { t, i18n } = useTranslation(['profile_new', 'common']);    const isEn = i18n.language === 'en';    const calculateAge = (dob: string) => {        if (!dob) return '---';        const birthDate = new Date(dob);        const today = new Date();        let age = today.getFullYear() - birthDate.getFullYear();        const m = today.getMonth() - birthDate.getMonth();        if (m < 0 || (m === 0 && today.getDate() < birthDate.getDate())) age--;        return age;    };    const getOptionLabel = (options: any[], value: any, bilingual?: boolean) => {        if (!value) return null;        const option = options.find(o => String(o.value) === String(value));        if (!option) return value;        if (bilingual) return getBilingualLabel(option.label, i18n.language as 'en' | 'ta');        return isEn ? option.label.en : option.label.ta;    };    const getLocationLabel = (type: 'district' | 'taluk', value: string) => {        if (!value || value === 'OTHER') return value;        if (!isEn) {            const tamilMap = type === 'district' ? DISTRICT_TAMIL : TALUK_TAMIL;            return tamilMap[value] || value;        }        return value.toLowerCase().split(' ').map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(' ');    };    const renderSection = (title: string, icon: string, items: { label: string; value: any; fullWidth?: boolean }[], children?: React.ReactNode) => (        <div className="bg-white border border-gold-soft/10 rounded-xl shadow-sm overflow-hidden transition-all hover:shadow-md h-full flex flex-col">            <div className="bg-ivory/50 px-6 py-3 border-b border-gold-soft flex items-center gap-3 shrink-0">                <div className="size-8 bg-rosewood-gradient text-white rounded-xl shadow-sm flex items-center justify-center text-rosewood">                    <span className="material-symbols-outlined text-base!">{icon}</span>                </div>                <h3 className="text-xs font-black tracking-widest text-rosewood uppercase">{title}</h3>            </div>            <div className="p-6 flex-1">                <div className="grid grid-cols-1 md:grid-cols-2 gap-y-4 gap-x-8">                    {items.map((item, idx) => (                        <div key={idx} className={`${item.fullWidth ? 'md:col-span-2' : ''} space-y-1`}>                            <p className="text-rosewood/80 font-heading font-semibold text-xs">{item.label}</p>                            <div className="text-[13px] font-bold text-gray-500 leading-relaxed">                                {(item.value !== undefined && item.value !== null && item.value !== '') ? item.value : (                                    <span className="text-gray-300 italic font-medium">{t('common:profile.not_specified')}</span>                                )}                            </div>                        </div>                    ))}                </div>                {children && <div className={items.length > 0 ? 'mt-8 pt-6 border-t border-gold-soft/10' : ''}>{children}</div>}            </div>        </div>    );    const isCreateMode = formData.astrology?.mode === 'GENERATED';    const horoscopeData = formData.astrology?.horoscopeJson        ? (typeof formData.astrology.horoscopeJson === 'string' ? JSON.parse(formData.astrology.horoscopeJson) : formData.astrology.horoscopeJson) as HoroscopeResult        : null;    const renderNoHoroscope = () => (        <div className="bg-ivory/30 border-2 border-dashed border-gold-soft/20 rounded-xl p-6 text-center">            <span className="material-symbols-outlined text-3xl text-slate-300 mb-1">auto_awesome_motion</span>            <p className="text-sm font-bold text-slate-400 italic">{t('common:profile.not_specified')}</p>        </div>    );    const renderRasiChartContent = () => {        if (isCreateMode && horoscopeData) {            return <D1Chart lagnaSign={horoscopeData.lagna.signIndex} planets={horoscopeData.planets} />;        }        return (            <div className="aspect-square rounded-xl overflow-hidden bg-white shadow-sm ring-4 ring-white">                {formData.astrology?.rasiChartUploadId ? (                    <MediaImage                        {...(formData.astrology?.rasiChartUploadUrl                            ? { image: { url: formData.astrology.rasiChartUploadUrl } }                            : { uploadId: formData.astrology.rasiChartUploadId }                        )}                        alt="Rasi"                        className="w-full h-full object-contain"                    />                ) : (                    <div className="w-full h-full flex items-center justify-center text-slate-300 italic text-xs">                        {t('common:profile.not_specified')}                    </div>                )}            </div>        );    };    const renderNavamsaChartContent = () => {        if (isCreateMode && horoscopeData) {            return <D9Chart planets={horoscopeData.planets} lagnaNavamsaSignIndex={horoscopeData.lagnaNavamsa.signIndex} />;        }        return (            <div className="aspect-square rounded-xl overflow-hidden bg-white shadow-sm ring-4 ring-white">                {formData.astrology?.navamsaChartUploadId ? (                    <MediaImage                        {...(formData.astrology?.navamsaChartUploadUrl                            ? { image: { url: formData.astrology.navamsaChartUploadUrl } }                            : { uploadId: formData.astrology.navamsaChartUploadId }                        )}                        alt="Navamsa"                        className="w-full h-full object-contain"                    />                ) : (                    <div className="w-full h-full flex items-center justify-center text-slate-300 italic text-xs">                        {t('common:profile.not_specified')}                    </div>                )}            </div>        );    };    const renderHoroscopeContent = () => {        if (!formData.astrology?.mode) {            return renderNoHoroscope();        }        return (            <div className="grid grid-cols-1 md:grid-cols-2 gap-8 lg:px-12">                <div className="space-y-4">                    <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest text-center">                        {t('profile_new:horoscope.rasi_chart')}                    </p>                    <div className="bg-ivory/20 rounded-xl p-4 ring-1 ring-gold-soft/10 shadow-inner">                        {renderRasiChartContent()}                    </div>                </div>                <div className="space-y-4">                    <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest text-center">                        {t('profile_new:horoscope.navamsa_chart')}                    </p>                    <div className="bg-ivory/20 rounded-xl p-4 ring-1 ring-gold-soft/10 shadow-inner">                        {renderNavamsaChartContent()}                    </div>                </div>            </div>        );    };    const renderMediaContent = () => {        if (!formData.primaryUploadId && (formData.galleryUploadIds?.length ?? 0) === 0) {            return (                <div className="bg-ivory/30 border-2 border-dashed border-gold-soft/20 rounded-xl p-6 text-center">                    <span className="material-symbols-outlined text-4xl text-slate-300 mb-2">no_photography</span>                    <p className="text-sm font-bold text-slate-400 italic">{t('common:profile.not_specified')}</p>                </div>            );        }        return (            <div>                {formData.primaryUploadId && (                    <div className="mb-4">                        <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest mb-2">Glimpse</p>                        <div className="w-28 aspect-4/5 rounded-xl overflow-hidden bg-slate-100 ring-2 ring-white shadow-md">                            <MediaImage                                {...(formData.primaryUploadUrl                                    ? { image: { url: formData.primaryUploadUrl } }                                    : { uploadId: formData.primaryUploadId }                                )}                                alt="Glimpse"                                className="w-full h-full object-cover"                            />                        </div>                    </div>                )}                {(formData.galleryUploadIds?.length ?? 0) > 0 && (                    <div>                        <p className="text-[10px] font-black text-rosewood/40 uppercase tracking-widest mb-2">                            {t('profile_new:gallery.lifestyle_title')}                        </p>                        <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">                            {formData.galleryUploadIds.map((id: string, idx: number) => (                                <div key={`gallery-${idx}`} className="aspect-[3/4] rounded-xl overflow-hidden bg-slate-100 ring-2 ring-white shadow-md">                                    <MediaImage                                        {...(formData.galleryUploadUrls?.[idx]                                            ? { image: { url: formData.galleryUploadUrls[idx] } }                                            : { uploadId: id }                                        )}                                        alt={`${t('profile_new:gallery.lifestyle_title')} ${idx + 1}`}                                        className="w-full h-full object-cover"                                    />                                </div>                            ))}                        </div>                    </div>                )}            </div>        );    };    return (        <div className="space-y-6 animate-in fade-in slide-in-from-right-4 duration-700 pb-10">            <div className="bg-ivory border border-gold/20 rounded-xl p-4 flex items-center gap-4 shadow-sm">                <div className="size-9 bg-rosewood/5 rounded-lg flex items-center justify-center text-rosewood shrink-0">                    <span className="material-symbols-outlined text-lg!">info</span>                </div>                <div>                    <p className="text-[10px] font-black tracking-widest text-rosewood/40 uppercase">{t('profile_new:review.warning_title')}</p>                    <p className="text-[13px] font-bold text-rosewood tracking-wide leading-snug">{t('profile_new:review.warning_desc')}</p>                </div>            </div>            <div className="bg-white rounded-2xl overflow-hidden shadow-sm ring-1 ring-gold-soft/20">                <div className="flex flex-col md:flex-row">                    <div className="md:w-72 shrink-0 bg-linear-to-br from-rosewood/5 to-ivory flex items-center justify-center p-6 md:p-8">                        <div className="w-32 h-40 md:w-44 md:h-56 rounded-2xl overflow-hidden ring-4 ring-white/80 shadow-xl bg-ivory">                            {(formData as any).primaryUploadId ? (                                <MediaImage                                    {...((formData as any).primaryUploadUrl                                        ? { image: { url: (formData as any).primaryUploadUrl } }                                        : { uploadId: (formData as any).primaryUploadId }                                    )}                                    alt="Profile"                                    className="w-full h-full object-cover"                                />                            ) : (                                <div className="w-full h-full flex items-center justify-center bg-ivory text-rosewood/20 text-5xl font-serif font-black">                                    {(formData.firstNameEn || formData.firstNameTa || '?')[0].toUpperCase()}                                </div>                            )}                        </div>                    </div>                    <div className="flex-1 p-6 md:p-8">                        <h2 className="text-2xl md:text-3xl font-serif font-bold text-rosewood">                            {isEn                                ? ([formData.firstNameEn, formData.lastNameEn].filter(Boolean).join(' ') || t('common:profile.not_specified'))                                : ([formData.firstNameTa, formData.lastNameTa].filter(Boolean).join(' ')                                    || [formData.firstNameEn, formData.lastNameEn].filter(Boolean).join(' ')                                    || t('common:profile.not_specified'))}                        </h2>                        <div className="flex flex-wrap gap-2 mt-3">                            <span className="inline-flex items-center gap-1.5 px-3 py-1.5 bg-rosewood text-white rounded-lg text-[10px] font-black tracking-wide uppercase shadow-sm shadow-rosewood/10">                                {formData.dob ? `${calculateAge(formData.dob)} ${t('common:yrs')}` : '---'}                            </span>                            <span className="inline-flex items-center gap-1.5 px-3 py-1.5 bg-rosewood/5 text-rosewood border border-rosewood/10 rounded-lg text-[10px] font-black tracking-wide uppercase">                                {getOptionLabel(GENDER_OPTIONS, formData.gender) || t('common:profile.not_specified')}                            </span>                            <span className="inline-flex items-center gap-1.5 px-3 py-1.5 bg-rosewood/5 text-rosewood border border-rosewood/10 rounded-lg text-[10px] font-black tracking-wide uppercase">                                {getOptionLabel(HEIGHT_OPTIONS, formData.height) || t('common:profile.not_specified')}                            </span>                            <span className="inline-flex items-center gap-1.5 px-3 py-1.5 bg-rosewood/5 text-rosewood border border-rosewood/10 rounded-lg text-[10px] font-black tracking-wide uppercase">                                {formData.weight ? `${formData.weight} kg` : t('common:profile.not_specified')}                            </span>                        </div>                        <div className="mt-6 grid grid-cols-2 md:grid-cols-3 gap-x-8 gap-y-4">                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:profile_for')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{getOptionLabel(PROFILE_FOR_OPTIONS, formData.profileFor) || <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:dob')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{formData.dob ? new Date(formData.dob).toLocaleDateString(i18n.language, { day: 'numeric', month: 'long', year: 'numeric' }) : <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:marital_status')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{getOptionLabel(MARITAL_STATUS_OPTIONS, formData.maritalStatus) || <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:blood_group')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{getOptionLabel(BLOOD_GROUP_OPTIONS, formData.bloodGroup) || <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:diet')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{getOptionLabel(DIET_OPTIONS, formData.diet) || <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                            <div className="space-y-1">                                <p className="text-rosewood/80 font-heading font-semibold text-[10px]">{t('profile_new:complexion')}</p>                                <p className="text-sm font-bold text-slate-700 leading-relaxed">{getOptionLabel(COMPLEXION_OPTIONS, formData.complexion) || <span className="text-slate-300 italic font-medium">{t('common:profile.not_specified')}</span>}</p>                            </div>                        </div>                    </div>                </div>            </div>            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">                {renderSection(t('profile_new:sections.community'), 'temple_hindu', [                    { label: t('profile_new:caste'), value: t('profile_new:caste_val') },                    { label: t('profile_new:community'), value: t('profile_new:community_val') },                    { label: t('profile_new:kulam'), value: getOptionLabel(KULAM_OPTIONS, formData.kulam) },                    { label: t('profile_new:kuladeivam'), value: isEn ? formData.kuladeivamEn : (formData.kuladeivamTa || formData.kuladeivamEn) },                ])}                {renderSection(t('profile_new:professional_details'), 'work', [                    { label: t('profile_new:education'), value: formData.education, fullWidth: true },                    { label: t('profile_new:job_sector'), value: getOptionLabel(JOB_SECTOR_OPTIONS, formData.jobSector) },                    { label: t('profile_new:job_detail'), value: formData.jobDetail },                    { label: t('profile_new:company_name'), value: formData.companyName },                    { label: t('profile_new:salary_monthly'), value: formData.salaryMonthly ? formatCurrency(formData.salaryMonthly) : null },                    { label: t('profile_new:job_location'), value: isEn ? formData.jobLocationEn : (formData.jobLocationTa || formData.jobLocationEn) },                ])}                {renderSection(t('profile_new:sections.locations'), 'location_on', [                    ...(formData.currentDistrict === 'OTHER' ? [                        { label: t('profile_new:current_city'), value: isEn ? formData.currentCityEn : (formData.currentCityTa || formData.currentCityEn) },                        { label: t('profile_new:current_state'), value: isEn ? formData.currentStateEn : (formData.currentStateTa || formData.currentStateEn) },                        { label: t('profile_new:current_country'), value: isEn ? formData.currentCountryEn : (formData.currentCountryTa || formData.currentCountryEn) },                    ] : [                        { label: t('profile_new:current_district'), value: formData.currentDistrict ? getLocationLabel('district', formData.currentDistrict) : null },                        { label: t('profile_new:current_taluk'), value: formData.currentTaluk ? getLocationLabel('taluk', formData.currentTaluk) : null },                    ]),                    { label: t('profile_new:native_district'), value: formData.nativeDistrict && formData.nativeDistrict !== 'OTHER' ? getLocationLabel('district', formData.nativeDistrict) : null },                    { label: t('profile_new:native_taluk'), value: formData.nativeTaluk ? getLocationLabel('taluk', formData.nativeTaluk) : null },                ])}                {renderSection(t('profile_new:family_details'), 'family_restroom', [                    { label: t('profile_new:father_name'), value: (() => { const n = isEn ? formData.fatherNameEn : (formData.fatherNameTa || formData.fatherNameEn); return n ? n + (formData.fatherIsLate ? ` (${t('profile_new:is_late')})` : '') : null; })() },                    { label: t('profile_new:father_job'), value: formData.fatherJob },                    { label: t('profile_new:mother_name'), value: (() => { const n = isEn ? formData.motherNameEn : (formData.motherNameTa || formData.motherNameEn); return n ? n + (formData.motherIsLate ? ` (${t('profile_new:is_late')})` : '') : null; })() },                    { label: t('profile_new:mother_job'), value: formData.motherJob },                    { label: t('profile_new:no_of_brothers'), value: formData.noOfBrothers ?? 0 },                    { label: t('profile_new:no_of_sisters'), value: formData.noOfSisters ?? 0 },                    { label: t('profile_new:father_salary'), value: formData.fatherSalary ? formatCurrency(formData.fatherSalary) : null },                    { label: t('profile_new:mother_salary'), value: formData.motherSalary ? formatCurrency(formData.motherSalary) : null },                ])}                {renderSection(t('profile_new:sections.partner_preferences'), 'favorite', [                    { label: t('profile_new:age_range'), value: formData.ageMin && formData.ageMax ? `${formData.ageMin} - ${formData.ageMax} ${t('common:yrs')}` : (formData.ageMin ? `${formData.ageMin}+` : (formData.ageMax ? `Up to ${formData.ageMax}` : null)) },                    { label: t('profile_new:height_min'), value: getOptionLabel(HEIGHT_OPTIONS, formData.heightMinId) },                    { label: t('profile_new:height_max'), value: getOptionLabel(HEIGHT_OPTIONS, formData.heightMaxId) },                    { label: t('profile_new:monthly_salary'), value: formData.monthlySalary ? formatCurrency(formData.monthlySalary) : null },                    { label: t('profile_new:preferred_location'), value: isEn ? formData.preferredLocationEn : (formData.preferredLocationTa || formData.preferredLocationEn), fullWidth: true },                    { label: t('profile_new:expectation'), value: formData.expectation, fullWidth: true },                ])}                {renderSection(t('profile_new:sections.property'), 'real_estate_agent', [                    { label: t('profile_new:residence'), value: getOptionLabel(RESIDENCE_OPTIONS, formData.residence) },                    { label: t('profile_new:vehicle'), value: formData.vehicle },                    { label: t('profile_new:land'), value: formData.land },                    { label: t('profile_new:other_assets'), value: formData.otherAssets },                ])}            </div>            <div className="w-full">                {renderSection(t('profile_new:sections.horoscope_main'), 'auto_awesome', [                    { label: t('profile_new:star'), value: getOptionLabel(NAKSHATRA_OPTIONS, formData.star, true) },                    { label: t('profile_new:rasi'), value: getOptionLabel(RASI_OPTIONS, formData.rasi, true) },                    { label: t('profile_new:laganam'), value: getOptionLabel(RASI_OPTIONS, formData.laganam, true) },                    { label: t('profile_new:review.chart_generation'), value: isCreateMode ? t('common:create') : t('common:upload') },                ], renderHoroscopeContent())}            </div>            <div className="w-full">                {renderSection(t('profile_new:sections.media'), 'collections', [], renderMediaContent())}            </div>            <label className="bg-white border border-gold-soft/10 rounded-xl shadow-sm p-4 flex items-center gap-3 cursor-pointer transition-all hover:border-gold/30">                <div className="relative flex items-center shrink-0">                    <input type="checkbox" checked={formData.agreedToTerms || false} onChange={(e) => updateField('agreedToTerms', e.target.checked)} className="peer hidden" />                    <div className={`size-4 rounded-md flex items-center justify-center transition-all ${formData.agreedToTerms ? 'bg-rosewood text-white shadow-sm' : 'bg-white border border-gold-soft/30'}`}>                        {formData.agreedToTerms && <span className="material-symbols-outlined text-xs!">check</span>}                    </div>                </div>                <span className="text-xs text-slate-600 leading-snug"><span className="font-bold text-rosewood">{t('profile_new:review.terms_notice')}</span><br /><span className="text-slate-400">{t('profile_new:review.terms_sub')}</span></span>            </label>            {fieldErrors.agreedToTerms && (                <p className="text-[10px] font-bold text-red-500 ml-2 mt-1 animate-in fade-in slide-in-from-top-1">{fieldErrors.agreedToTerms}</p>            )}        </div>    );};export default Step7Review;