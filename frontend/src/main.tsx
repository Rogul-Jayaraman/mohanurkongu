import React from 'react'import ReactDOM from 'react-dom/client'import * as Sentry from '@sentry/react'import { QueryClient, QueryClientProvider } from '@tanstack/react-query'import App from './App'import './assets/styles/manamaalai.css'import './assets/styles/index.css'import i18n from './i18n'import { I18nextProvider } from 'react-i18next'if (import.meta.env.VITE_SENTRY_DSN) {  Sentry.init({    dsn: import.meta.env.VITE_SENTRY_DSN,    environment: import.meta.env.VITE_APP_ENV || 'development',    tracesSampleRate: parseFloat(import.meta.env.VITE_SENTRY_TRACES_SAMPLE_RATE || '0.2'),  });}const queryClient = new QueryClient({  defaultOptions: {    queries: {      retry: (failureCount, error: any) => {        const status = error?.status ?? error?.response?.status;        if (status === 401 || status === 403 || status === 404) return false;        if (status >= 500) return failureCount < 2;        return failureCount < 1;      },      refetchOnWindowFocus: false,      refetchOnReconnect: true,      staleTime: 30_000,      gcTime: 5 * 60_000,    },    mutations: {      retry: false,    },  },});// Disable automatic scroll restoration and scroll to top on page loadif ('scrollRestoration' in window.history) {  window.history.scrollRestoration = 'manual';}window.scrollTo(0, 0);document.addEventListener('wheel', () => {    if (document.activeElement instanceof HTMLInputElement && document.activeElement.type === 'number') {        document.activeElement.blur();    }});window.onerror = (_msg, _url, _line, _col, err) => {  console.error('Uncaught error:', err);};window.addEventListener('unhandledrejection', (e) => {  console.error('Unhandled promise rejection:', e.reason);});const Root = () => {  const app = (    <QueryClientProvider client={queryClient}>      <I18nextProvider i18n={i18n}>        <App />      </I18nextProvider>    </QueryClientProvider>  );  if (import.meta.env.VITE_SENTRY_DSN) {    return (      <React.StrictMode>        <Sentry.ErrorBoundary fallback={<div>Something went wrong</div>}>          {app}        </Sentry.ErrorBoundary>      </React.StrictMode>    );  }  return <React.StrictMode>{app}</React.StrictMode>;};ReactDOM.createRoot(document.getElementById('root')!).render(<Root />)