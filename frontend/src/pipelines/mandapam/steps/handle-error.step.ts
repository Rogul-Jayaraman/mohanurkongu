import type { MandapamFrontendContext } from '../context.types';export function handleErrorStep<TInput, TOutput>(  errorHandler: (errors: Array<{ step: string; error: unknown }>) => void,) {  return async (    ctx: MandapamFrontendContext<TInput, TOutput>,  ): Promise<MandapamFrontendContext<TInput, TOutput>> => {    if (ctx.errors.length > 0) {      errorHandler(ctx.errors);    }    return ctx;  };}