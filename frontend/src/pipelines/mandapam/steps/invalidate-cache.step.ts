import type { QueryClient } from '@tanstack/react-query';import type { MandapamFrontendContext } from '../context.types';export function invalidateCacheStep<TInput, TOutput>(  queryClient: QueryClient,  queryKeys: Array<readonly unknown[]>,) {  return async (    ctx: MandapamFrontendContext<TInput, TOutput>,  ): Promise<MandapamFrontendContext<TInput, TOutput>> => {    for (const key of queryKeys) {      queryClient.invalidateQueries({ queryKey: key });    }    return ctx;  };}