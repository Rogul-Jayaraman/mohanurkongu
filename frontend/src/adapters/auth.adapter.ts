import type { BackendAccount } from '../api/auth.api';import type { User, Admin } from '../types/user';export function decodeJwtPayload(token: string) {  return JSON.parse(atob(token.split('.')[1]));}export function mapAccountToUser(account: BackendAccount, accessToken: string): User {  const { sub, roles } = decodeJwtPayload(accessToken);  return {    id: sub,    customId: account.accountNo,    firstNameEn: account.firstNameEn,    lastNameEn: account.lastNameEn,    firstNameTa: account.firstNameTa || null,    lastNameTa: account.lastNameTa || null,    email: account.email,    phone: account.phone || '',    role: roles.includes('ADMIN') ? 'ADMIN' : 'USER',    membership: account.membership,    createdAt: account.createdAt,    updatedAt: account.createdAt,  };}export function mapAccountToAdmin(account: BackendAccount, accessToken: string): Admin {  const { sub } = decodeJwtPayload(accessToken);  return {    id: sub,    customId: account.accountNo,    firstNameEn: account.firstNameEn,    lastNameEn: account.lastNameEn,    firstNameTa: account.firstNameTa || null,    lastNameTa: account.lastNameTa || null,    email: account.email,    phone: account.phone || '',    role: 'ADMIN',    createdAt: account.createdAt,  };}export function storeSession(accessToken: string, account: BackendAccount) {  const { roles } = decodeJwtPayload(accessToken);  const isAdmin = roles.includes('ADMIN');  return isAdmin ? mapAccountToAdmin(account, accessToken) : mapAccountToUser(account, accessToken);}export function clearSession() {  // No-op — session is memory-only, managed by useAuth + session.ts}